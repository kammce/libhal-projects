
// Copyright 2023 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//      http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

#include <array>
#include <cstdint>
#include <cstdlib>

#include <tl/expected.hpp>

constexpr size_t error_size = 20;
constexpr bool error_in_constructor = true;
constexpr bool error_in_class_function = true;
constexpr uint8_t depth_before_exception = 50;

struct my_error_t
{
  std::array<std::uint8_t, error_size> data;
};

volatile std::uint64_t enable_register;
volatile std::uint64_t trigger_register;

class non_trivial_destructor
{
public:
  static tl::expected<non_trivial_destructor, my_error_t> initialize(
    uint8_t p_channel)
  {
    if constexpr (error_in_constructor) {
      if (p_channel >= depth_before_exception) {
        return tl::unexpected(my_error_t{ .data = { 0x55, 0xAA, 0x33, 0x44 } });
      }
    }
    enable_register = enable_register | (1 << p_channel);
    return non_trivial_destructor{ p_channel };
  }

  non_trivial_destructor(non_trivial_destructor&) = delete;
  non_trivial_destructor& operator=(non_trivial_destructor&) = delete;
  non_trivial_destructor(non_trivial_destructor&&) noexcept = default;
  non_trivial_destructor& operator=(non_trivial_destructor&&) noexcept =
    default;

  tl::expected<void, my_error_t> trigger()
  {
    if constexpr (error_in_class_function) {
      if (m_channel >= depth_before_exception) {
        tl::unexpected(my_error_t{ .data = { 0xAA, 0xBB, 0x33, 0x44 } });
      }
    }
    trigger_register = trigger_register | (1 << (m_channel % 64));

    return {};
  }

  ~non_trivial_destructor()
  {
    enable_register = enable_register & ~(1 << (m_channel % 64));
  }

private:
  non_trivial_destructor(uint8_t p_channel)
    : m_channel(p_channel)
  {
  }

  uint8_t m_channel = 0;
};

tl::expected<int, my_error_t> return_error();
tl::expected<int, my_error_t> top_call()
{
  auto result = return_error();
  if (!result) {
    return tl::unexpected(result.error());
  }
  return result.value();
}

int main()
{
  auto result = top_call();
  if (!result) {
    return static_cast<int>(result.error().data[0]);
  }
  return result.value();
}

// =============================================================================
// Add generated code below
// =============================================================================

tl::expected<int, my_error_t> fallible_function0_group0();
volatile int side_effect0 = 0;

tl::expected<int, my_error_t> fallible_function0_group1();
volatile int side_effect1 = 0;

tl::expected<int, my_error_t> fallible_function0_group2();
volatile int side_effect2 = 0;

tl::expected<int, my_error_t> fallible_function0_group3();
volatile int side_effect3 = 0;

tl::expected<int, my_error_t> fallible_function0_group4();
volatile int side_effect4 = 0;

tl::expected<int, my_error_t> fallible_function0_group5();
volatile int side_effect5 = 0;

tl::expected<int, my_error_t> fallible_function0_group6();
volatile int side_effect6 = 0;

tl::expected<int, my_error_t> fallible_function0_group7();
volatile int side_effect7 = 0;

tl::expected<int, my_error_t> fallible_function0_group8();
volatile int side_effect8 = 0;

tl::expected<int, my_error_t> fallible_function0_group9();
volatile int side_effect9 = 0;

tl::expected<int, my_error_t> fallible_function0_group10();
volatile int side_effect10 = 0;

tl::expected<int, my_error_t> fallible_function0_group11();
volatile int side_effect11 = 0;

tl::expected<int, my_error_t> fallible_function0_group12();
volatile int side_effect12 = 0;

tl::expected<int, my_error_t> fallible_function0_group13();
volatile int side_effect13 = 0;

tl::expected<int, my_error_t> fallible_function0_group14();
volatile int side_effect14 = 0;

tl::expected<int, my_error_t> fallible_function0_group15();
volatile int side_effect15 = 0;

tl::expected<int, my_error_t> fallible_function0_group16();
volatile int side_effect16 = 0;

tl::expected<int, my_error_t> fallible_function0_group17();
volatile int side_effect17 = 0;

tl::expected<int, my_error_t> fallible_function0_group18();
volatile int side_effect18 = 0;

tl::expected<int, my_error_t> fallible_function0_group19();
volatile int side_effect19 = 0;

tl::expected<int, my_error_t> fallible_function0_group20();
volatile int side_effect20 = 0;

tl::expected<int, my_error_t> fallible_function0_group21();
volatile int side_effect21 = 0;

tl::expected<int, my_error_t> fallible_function0_group22();
volatile int side_effect22 = 0;

tl::expected<int, my_error_t> fallible_function0_group23();
volatile int side_effect23 = 0;

tl::expected<int, my_error_t> fallible_function0_group24();
volatile int side_effect24 = 0;

tl::expected<int, my_error_t> fallible_function0_group25();
volatile int side_effect25 = 0;

tl::expected<int, my_error_t> fallible_function0_group26();
volatile int side_effect26 = 0;

tl::expected<int, my_error_t> fallible_function0_group27();
volatile int side_effect27 = 0;

tl::expected<int, my_error_t> fallible_function0_group28();
volatile int side_effect28 = 0;

tl::expected<int, my_error_t> fallible_function0_group29();
volatile int side_effect29 = 0;

tl::expected<int, my_error_t> fallible_function0_group30();
volatile int side_effect30 = 0;

tl::expected<int, my_error_t> fallible_function0_group31();
volatile int side_effect31 = 0;

tl::expected<int, my_error_t> fallible_function0_group32();
volatile int side_effect32 = 0;

tl::expected<int, my_error_t> fallible_function0_group33();
volatile int side_effect33 = 0;

tl::expected<int, my_error_t> fallible_function0_group34();
volatile int side_effect34 = 0;

tl::expected<int, my_error_t> fallible_function0_group35();
volatile int side_effect35 = 0;

tl::expected<int, my_error_t> fallible_function0_group36();
volatile int side_effect36 = 0;

tl::expected<int, my_error_t> fallible_function0_group37();
volatile int side_effect37 = 0;

tl::expected<int, my_error_t> fallible_function0_group38();
volatile int side_effect38 = 0;

tl::expected<int, my_error_t> fallible_function0_group39();
volatile int side_effect39 = 0;

tl::expected<int, my_error_t> return_error()
{

  if (auto result = fallible_function0_group0(); !result) {
    return tl::unexpected(result.error());
  }

  if (auto result = fallible_function0_group1(); !result) {
    return tl::unexpected(result.error());
  }

  if (auto result = fallible_function0_group2(); !result) {
    return tl::unexpected(result.error());
  }

  if (auto result = fallible_function0_group3(); !result) {
    return tl::unexpected(result.error());
  }

  if (auto result = fallible_function0_group4(); !result) {
    return tl::unexpected(result.error());
  }

  if (auto result = fallible_function0_group5(); !result) {
    return tl::unexpected(result.error());
  }

  if (auto result = fallible_function0_group6(); !result) {
    return tl::unexpected(result.error());
  }

  if (auto result = fallible_function0_group7(); !result) {
    return tl::unexpected(result.error());
  }

  if (auto result = fallible_function0_group8(); !result) {
    return tl::unexpected(result.error());
  }

  if (auto result = fallible_function0_group9(); !result) {
    return tl::unexpected(result.error());
  }

  if (auto result = fallible_function0_group10(); !result) {
    return tl::unexpected(result.error());
  }

  if (auto result = fallible_function0_group11(); !result) {
    return tl::unexpected(result.error());
  }

  if (auto result = fallible_function0_group12(); !result) {
    return tl::unexpected(result.error());
  }

  if (auto result = fallible_function0_group13(); !result) {
    return tl::unexpected(result.error());
  }

  if (auto result = fallible_function0_group14(); !result) {
    return tl::unexpected(result.error());
  }

  if (auto result = fallible_function0_group15(); !result) {
    return tl::unexpected(result.error());
  }

  if (auto result = fallible_function0_group16(); !result) {
    return tl::unexpected(result.error());
  }

  if (auto result = fallible_function0_group17(); !result) {
    return tl::unexpected(result.error());
  }

  if (auto result = fallible_function0_group18(); !result) {
    return tl::unexpected(result.error());
  }

  if (auto result = fallible_function0_group19(); !result) {
    return tl::unexpected(result.error());
  }

  if (auto result = fallible_function0_group20(); !result) {
    return tl::unexpected(result.error());
  }

  if (auto result = fallible_function0_group21(); !result) {
    return tl::unexpected(result.error());
  }

  if (auto result = fallible_function0_group22(); !result) {
    return tl::unexpected(result.error());
  }

  if (auto result = fallible_function0_group23(); !result) {
    return tl::unexpected(result.error());
  }

  if (auto result = fallible_function0_group24(); !result) {
    return tl::unexpected(result.error());
  }

  if (auto result = fallible_function0_group25(); !result) {
    return tl::unexpected(result.error());
  }

  if (auto result = fallible_function0_group26(); !result) {
    return tl::unexpected(result.error());
  }

  if (auto result = fallible_function0_group27(); !result) {
    return tl::unexpected(result.error());
  }

  if (auto result = fallible_function0_group28(); !result) {
    return tl::unexpected(result.error());
  }

  if (auto result = fallible_function0_group29(); !result) {
    return tl::unexpected(result.error());
  }

  if (auto result = fallible_function0_group30(); !result) {
    return tl::unexpected(result.error());
  }

  if (auto result = fallible_function0_group31(); !result) {
    return tl::unexpected(result.error());
  }

  if (auto result = fallible_function0_group32(); !result) {
    return tl::unexpected(result.error());
  }

  if (auto result = fallible_function0_group33(); !result) {
    return tl::unexpected(result.error());
  }

  if (auto result = fallible_function0_group34(); !result) {
    return tl::unexpected(result.error());
  }

  if (auto result = fallible_function0_group35(); !result) {
    return tl::unexpected(result.error());
  }

  if (auto result = fallible_function0_group36(); !result) {
    return tl::unexpected(result.error());
  }

  if (auto result = fallible_function0_group37(); !result) {
    return tl::unexpected(result.error());
  }

  if (auto result = fallible_function0_group38(); !result) {
    return tl::unexpected(result.error());
  }

  if (auto result = fallible_function0_group39(); !result) {
    return tl::unexpected(result.error());
  }
  return side_effect0 + side_effect1 + side_effect2 + side_effect3 +
         side_effect4 + side_effect5 + side_effect6 + side_effect7 +
         side_effect8 + side_effect9 + side_effect10 + side_effect11 +
         side_effect12 + side_effect13 + side_effect14 + side_effect15 +
         side_effect16 + side_effect17 + side_effect18 + side_effect19 +
         side_effect20 + side_effect21 + side_effect22 + side_effect23 +
         side_effect24 + side_effect25 + side_effect26 + side_effect27 +
         side_effect28 + side_effect29 + side_effect30 + side_effect31 +
         side_effect32 + side_effect33 + side_effect34 + side_effect35 +
         side_effect36 + side_effect37 + side_effect38 + side_effect39;
}

tl::expected<int, my_error_t> fallible_function1_group0();

tl::expected<int, my_error_t> fallible_function0_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function1_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function2_group0();

tl::expected<int, my_error_t> fallible_function1_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function2_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function3_group0();

tl::expected<int, my_error_t> fallible_function2_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function3_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function4_group0();

tl::expected<int, my_error_t> fallible_function3_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function4_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function5_group0();

tl::expected<int, my_error_t> fallible_function4_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function5_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function6_group0();

tl::expected<int, my_error_t> fallible_function5_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function6_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function7_group0();

tl::expected<int, my_error_t> fallible_function6_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function7_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function8_group0();

tl::expected<int, my_error_t> fallible_function7_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function8_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function9_group0();

tl::expected<int, my_error_t> fallible_function8_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function9_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function10_group0();

tl::expected<int, my_error_t> fallible_function9_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function10_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function11_group0();

tl::expected<int, my_error_t> fallible_function10_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function11_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function12_group0();

tl::expected<int, my_error_t> fallible_function11_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function12_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function13_group0();

tl::expected<int, my_error_t> fallible_function12_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function13_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function14_group0();

tl::expected<int, my_error_t> fallible_function13_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function14_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function15_group0();

tl::expected<int, my_error_t> fallible_function14_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function15_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function16_group0();

tl::expected<int, my_error_t> fallible_function15_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function16_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function17_group0();

tl::expected<int, my_error_t> fallible_function16_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function17_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function18_group0();

tl::expected<int, my_error_t> fallible_function17_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function18_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function19_group0();

tl::expected<int, my_error_t> fallible_function18_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function19_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function20_group0();

tl::expected<int, my_error_t> fallible_function19_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function20_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function21_group0();

tl::expected<int, my_error_t> fallible_function20_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function21_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function22_group0();

tl::expected<int, my_error_t> fallible_function21_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function22_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function23_group0();

tl::expected<int, my_error_t> fallible_function22_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function23_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function24_group0();

tl::expected<int, my_error_t> fallible_function23_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function24_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function25_group0();

tl::expected<int, my_error_t> fallible_function24_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function25_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function26_group0();

tl::expected<int, my_error_t> fallible_function25_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function26_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function27_group0();

tl::expected<int, my_error_t> fallible_function26_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function27_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function28_group0();

tl::expected<int, my_error_t> fallible_function27_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function28_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function29_group0();

tl::expected<int, my_error_t> fallible_function28_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function29_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function30_group0();

tl::expected<int, my_error_t> fallible_function29_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function30_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function31_group0();

tl::expected<int, my_error_t> fallible_function30_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function31_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function32_group0();

tl::expected<int, my_error_t> fallible_function31_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function32_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function33_group0();

tl::expected<int, my_error_t> fallible_function32_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function33_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function34_group0();

tl::expected<int, my_error_t> fallible_function33_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function34_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function35_group0();

tl::expected<int, my_error_t> fallible_function34_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function35_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function36_group0();

tl::expected<int, my_error_t> fallible_function35_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function36_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function37_group0();

tl::expected<int, my_error_t> fallible_function36_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function37_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function38_group0();

tl::expected<int, my_error_t> fallible_function37_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function38_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function39_group0();

tl::expected<int, my_error_t> fallible_function38_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function39_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function40_group0();

tl::expected<int, my_error_t> fallible_function39_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function40_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function41_group0();

tl::expected<int, my_error_t> fallible_function40_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function41_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function42_group0();

tl::expected<int, my_error_t> fallible_function41_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function42_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function43_group0();

tl::expected<int, my_error_t> fallible_function42_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function43_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function44_group0();

tl::expected<int, my_error_t> fallible_function43_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function44_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function45_group0();

tl::expected<int, my_error_t> fallible_function44_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function45_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function46_group0();

tl::expected<int, my_error_t> fallible_function45_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function46_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function47_group0();

tl::expected<int, my_error_t> fallible_function46_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function47_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function48_group0();

tl::expected<int, my_error_t> fallible_function47_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function48_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function49_group0();

tl::expected<int, my_error_t> fallible_function48_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function49_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function50_group0();

tl::expected<int, my_error_t> fallible_function49_group0()
{
  auto result_a = non_trivial_destructor::initialize(0);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(0);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function50_group0(); result) {
    side_effect0 = side_effect0 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0;
}

tl::expected<int, my_error_t> fallible_function50_group0()
{
  if (auto result = non_trivial_destructor::initialize(50); result) {
    auto result_internal = result.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect0 + 1;
}

tl::expected<int, my_error_t> fallible_function1_group1();

tl::expected<int, my_error_t> fallible_function0_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function1_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function2_group1();

tl::expected<int, my_error_t> fallible_function1_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function2_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function3_group1();

tl::expected<int, my_error_t> fallible_function2_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function3_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function4_group1();

tl::expected<int, my_error_t> fallible_function3_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function4_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function5_group1();

tl::expected<int, my_error_t> fallible_function4_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function5_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function6_group1();

tl::expected<int, my_error_t> fallible_function5_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function6_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function7_group1();

tl::expected<int, my_error_t> fallible_function6_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function7_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function8_group1();

tl::expected<int, my_error_t> fallible_function7_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function8_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function9_group1();

tl::expected<int, my_error_t> fallible_function8_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function9_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function10_group1();

tl::expected<int, my_error_t> fallible_function9_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function10_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function11_group1();

tl::expected<int, my_error_t> fallible_function10_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function11_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function12_group1();

tl::expected<int, my_error_t> fallible_function11_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function12_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function13_group1();

tl::expected<int, my_error_t> fallible_function12_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function13_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function14_group1();

tl::expected<int, my_error_t> fallible_function13_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function14_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function15_group1();

tl::expected<int, my_error_t> fallible_function14_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function15_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function16_group1();

tl::expected<int, my_error_t> fallible_function15_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function16_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function17_group1();

tl::expected<int, my_error_t> fallible_function16_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function17_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function18_group1();

tl::expected<int, my_error_t> fallible_function17_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function18_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function19_group1();

tl::expected<int, my_error_t> fallible_function18_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function19_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function20_group1();

tl::expected<int, my_error_t> fallible_function19_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function20_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function21_group1();

tl::expected<int, my_error_t> fallible_function20_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function21_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function22_group1();

tl::expected<int, my_error_t> fallible_function21_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function22_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function23_group1();

tl::expected<int, my_error_t> fallible_function22_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function23_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function24_group1();

tl::expected<int, my_error_t> fallible_function23_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function24_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function25_group1();

tl::expected<int, my_error_t> fallible_function24_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function25_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function26_group1();

tl::expected<int, my_error_t> fallible_function25_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function26_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function27_group1();

tl::expected<int, my_error_t> fallible_function26_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function27_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function28_group1();

tl::expected<int, my_error_t> fallible_function27_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function28_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function29_group1();

tl::expected<int, my_error_t> fallible_function28_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function29_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function30_group1();

tl::expected<int, my_error_t> fallible_function29_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function30_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function31_group1();

tl::expected<int, my_error_t> fallible_function30_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function31_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function32_group1();

tl::expected<int, my_error_t> fallible_function31_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function32_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function33_group1();

tl::expected<int, my_error_t> fallible_function32_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function33_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function34_group1();

tl::expected<int, my_error_t> fallible_function33_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function34_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function35_group1();

tl::expected<int, my_error_t> fallible_function34_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function35_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function36_group1();

tl::expected<int, my_error_t> fallible_function35_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function36_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function37_group1();

tl::expected<int, my_error_t> fallible_function36_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function37_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function38_group1();

tl::expected<int, my_error_t> fallible_function37_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function38_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function39_group1();

tl::expected<int, my_error_t> fallible_function38_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function39_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function40_group1();

tl::expected<int, my_error_t> fallible_function39_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function40_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function41_group1();

tl::expected<int, my_error_t> fallible_function40_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function41_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function42_group1();

tl::expected<int, my_error_t> fallible_function41_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function42_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function43_group1();

tl::expected<int, my_error_t> fallible_function42_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function43_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function44_group1();

tl::expected<int, my_error_t> fallible_function43_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function44_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function45_group1();

tl::expected<int, my_error_t> fallible_function44_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function45_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function46_group1();

tl::expected<int, my_error_t> fallible_function45_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function46_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function47_group1();

tl::expected<int, my_error_t> fallible_function46_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function47_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function48_group1();

tl::expected<int, my_error_t> fallible_function47_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function48_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function49_group1();

tl::expected<int, my_error_t> fallible_function48_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function49_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function50_group1();

tl::expected<int, my_error_t> fallible_function49_group1()
{
  auto result_a = non_trivial_destructor::initialize(1);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(1);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function50_group1(); result) {
    side_effect1 = side_effect1 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1;
}

tl::expected<int, my_error_t> fallible_function50_group1()
{
  if (auto result = non_trivial_destructor::initialize(50); result) {
    auto result_internal = result.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect1 + 1;
}

tl::expected<int, my_error_t> fallible_function1_group2();

tl::expected<int, my_error_t> fallible_function0_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function1_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function2_group2();

tl::expected<int, my_error_t> fallible_function1_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function2_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function3_group2();

tl::expected<int, my_error_t> fallible_function2_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function3_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function4_group2();

tl::expected<int, my_error_t> fallible_function3_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function4_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function5_group2();

tl::expected<int, my_error_t> fallible_function4_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function5_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function6_group2();

tl::expected<int, my_error_t> fallible_function5_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function6_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function7_group2();

tl::expected<int, my_error_t> fallible_function6_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function7_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function8_group2();

tl::expected<int, my_error_t> fallible_function7_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function8_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function9_group2();

tl::expected<int, my_error_t> fallible_function8_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function9_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function10_group2();

tl::expected<int, my_error_t> fallible_function9_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function10_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function11_group2();

tl::expected<int, my_error_t> fallible_function10_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function11_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function12_group2();

tl::expected<int, my_error_t> fallible_function11_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function12_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function13_group2();

tl::expected<int, my_error_t> fallible_function12_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function13_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function14_group2();

tl::expected<int, my_error_t> fallible_function13_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function14_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function15_group2();

tl::expected<int, my_error_t> fallible_function14_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function15_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function16_group2();

tl::expected<int, my_error_t> fallible_function15_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function16_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function17_group2();

tl::expected<int, my_error_t> fallible_function16_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function17_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function18_group2();

tl::expected<int, my_error_t> fallible_function17_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function18_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function19_group2();

tl::expected<int, my_error_t> fallible_function18_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function19_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function20_group2();

tl::expected<int, my_error_t> fallible_function19_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function20_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function21_group2();

tl::expected<int, my_error_t> fallible_function20_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function21_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function22_group2();

tl::expected<int, my_error_t> fallible_function21_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function22_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function23_group2();

tl::expected<int, my_error_t> fallible_function22_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function23_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function24_group2();

tl::expected<int, my_error_t> fallible_function23_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function24_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function25_group2();

tl::expected<int, my_error_t> fallible_function24_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function25_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function26_group2();

tl::expected<int, my_error_t> fallible_function25_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function26_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function27_group2();

tl::expected<int, my_error_t> fallible_function26_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function27_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function28_group2();

tl::expected<int, my_error_t> fallible_function27_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function28_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function29_group2();

tl::expected<int, my_error_t> fallible_function28_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function29_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function30_group2();

tl::expected<int, my_error_t> fallible_function29_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function30_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function31_group2();

tl::expected<int, my_error_t> fallible_function30_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function31_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function32_group2();

tl::expected<int, my_error_t> fallible_function31_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function32_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function33_group2();

tl::expected<int, my_error_t> fallible_function32_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function33_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function34_group2();

tl::expected<int, my_error_t> fallible_function33_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function34_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function35_group2();

tl::expected<int, my_error_t> fallible_function34_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function35_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function36_group2();

tl::expected<int, my_error_t> fallible_function35_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function36_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function37_group2();

tl::expected<int, my_error_t> fallible_function36_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function37_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function38_group2();

tl::expected<int, my_error_t> fallible_function37_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function38_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function39_group2();

tl::expected<int, my_error_t> fallible_function38_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function39_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function40_group2();

tl::expected<int, my_error_t> fallible_function39_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function40_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function41_group2();

tl::expected<int, my_error_t> fallible_function40_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function41_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function42_group2();

tl::expected<int, my_error_t> fallible_function41_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function42_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function43_group2();

tl::expected<int, my_error_t> fallible_function42_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function43_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function44_group2();

tl::expected<int, my_error_t> fallible_function43_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function44_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function45_group2();

tl::expected<int, my_error_t> fallible_function44_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function45_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function46_group2();

tl::expected<int, my_error_t> fallible_function45_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function46_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function47_group2();

tl::expected<int, my_error_t> fallible_function46_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function47_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function48_group2();

tl::expected<int, my_error_t> fallible_function47_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function48_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function49_group2();

tl::expected<int, my_error_t> fallible_function48_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function49_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function50_group2();

tl::expected<int, my_error_t> fallible_function49_group2()
{
  auto result_a = non_trivial_destructor::initialize(2);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(2);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function50_group2(); result) {
    side_effect2 = side_effect2 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2;
}

tl::expected<int, my_error_t> fallible_function50_group2()
{
  if (auto result = non_trivial_destructor::initialize(50); result) {
    auto result_internal = result.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect2 + 1;
}

tl::expected<int, my_error_t> fallible_function1_group3();

tl::expected<int, my_error_t> fallible_function0_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function1_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function2_group3();

tl::expected<int, my_error_t> fallible_function1_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function2_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function3_group3();

tl::expected<int, my_error_t> fallible_function2_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function3_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function4_group3();

tl::expected<int, my_error_t> fallible_function3_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function4_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function5_group3();

tl::expected<int, my_error_t> fallible_function4_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function5_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function6_group3();

tl::expected<int, my_error_t> fallible_function5_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function6_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function7_group3();

tl::expected<int, my_error_t> fallible_function6_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function7_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function8_group3();

tl::expected<int, my_error_t> fallible_function7_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function8_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function9_group3();

tl::expected<int, my_error_t> fallible_function8_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function9_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function10_group3();

tl::expected<int, my_error_t> fallible_function9_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function10_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function11_group3();

tl::expected<int, my_error_t> fallible_function10_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function11_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function12_group3();

tl::expected<int, my_error_t> fallible_function11_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function12_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function13_group3();

tl::expected<int, my_error_t> fallible_function12_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function13_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function14_group3();

tl::expected<int, my_error_t> fallible_function13_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function14_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function15_group3();

tl::expected<int, my_error_t> fallible_function14_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function15_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function16_group3();

tl::expected<int, my_error_t> fallible_function15_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function16_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function17_group3();

tl::expected<int, my_error_t> fallible_function16_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function17_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function18_group3();

tl::expected<int, my_error_t> fallible_function17_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function18_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function19_group3();

tl::expected<int, my_error_t> fallible_function18_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function19_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function20_group3();

tl::expected<int, my_error_t> fallible_function19_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function20_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function21_group3();

tl::expected<int, my_error_t> fallible_function20_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function21_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function22_group3();

tl::expected<int, my_error_t> fallible_function21_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function22_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function23_group3();

tl::expected<int, my_error_t> fallible_function22_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function23_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function24_group3();

tl::expected<int, my_error_t> fallible_function23_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function24_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function25_group3();

tl::expected<int, my_error_t> fallible_function24_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function25_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function26_group3();

tl::expected<int, my_error_t> fallible_function25_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function26_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function27_group3();

tl::expected<int, my_error_t> fallible_function26_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function27_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function28_group3();

tl::expected<int, my_error_t> fallible_function27_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function28_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function29_group3();

tl::expected<int, my_error_t> fallible_function28_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function29_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function30_group3();

tl::expected<int, my_error_t> fallible_function29_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function30_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function31_group3();

tl::expected<int, my_error_t> fallible_function30_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function31_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function32_group3();

tl::expected<int, my_error_t> fallible_function31_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function32_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function33_group3();

tl::expected<int, my_error_t> fallible_function32_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function33_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function34_group3();

tl::expected<int, my_error_t> fallible_function33_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function34_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function35_group3();

tl::expected<int, my_error_t> fallible_function34_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function35_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function36_group3();

tl::expected<int, my_error_t> fallible_function35_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function36_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function37_group3();

tl::expected<int, my_error_t> fallible_function36_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function37_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function38_group3();

tl::expected<int, my_error_t> fallible_function37_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function38_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function39_group3();

tl::expected<int, my_error_t> fallible_function38_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function39_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function40_group3();

tl::expected<int, my_error_t> fallible_function39_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function40_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function41_group3();

tl::expected<int, my_error_t> fallible_function40_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function41_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function42_group3();

tl::expected<int, my_error_t> fallible_function41_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function42_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function43_group3();

tl::expected<int, my_error_t> fallible_function42_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function43_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function44_group3();

tl::expected<int, my_error_t> fallible_function43_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function44_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function45_group3();

tl::expected<int, my_error_t> fallible_function44_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function45_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function46_group3();

tl::expected<int, my_error_t> fallible_function45_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function46_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function47_group3();

tl::expected<int, my_error_t> fallible_function46_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function47_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function48_group3();

tl::expected<int, my_error_t> fallible_function47_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function48_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function49_group3();

tl::expected<int, my_error_t> fallible_function48_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function49_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function50_group3();

tl::expected<int, my_error_t> fallible_function49_group3()
{
  auto result_a = non_trivial_destructor::initialize(3);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(3);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function50_group3(); result) {
    side_effect3 = side_effect3 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3;
}

tl::expected<int, my_error_t> fallible_function50_group3()
{
  if (auto result = non_trivial_destructor::initialize(50); result) {
    auto result_internal = result.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect3 + 1;
}

tl::expected<int, my_error_t> fallible_function1_group4();

tl::expected<int, my_error_t> fallible_function0_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function1_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function2_group4();

tl::expected<int, my_error_t> fallible_function1_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function2_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function3_group4();

tl::expected<int, my_error_t> fallible_function2_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function3_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function4_group4();

tl::expected<int, my_error_t> fallible_function3_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function4_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function5_group4();

tl::expected<int, my_error_t> fallible_function4_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function5_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function6_group4();

tl::expected<int, my_error_t> fallible_function5_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function6_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function7_group4();

tl::expected<int, my_error_t> fallible_function6_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function7_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function8_group4();

tl::expected<int, my_error_t> fallible_function7_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function8_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function9_group4();

tl::expected<int, my_error_t> fallible_function8_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function9_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function10_group4();

tl::expected<int, my_error_t> fallible_function9_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function10_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function11_group4();

tl::expected<int, my_error_t> fallible_function10_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function11_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function12_group4();

tl::expected<int, my_error_t> fallible_function11_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function12_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function13_group4();

tl::expected<int, my_error_t> fallible_function12_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function13_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function14_group4();

tl::expected<int, my_error_t> fallible_function13_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function14_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function15_group4();

tl::expected<int, my_error_t> fallible_function14_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function15_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function16_group4();

tl::expected<int, my_error_t> fallible_function15_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function16_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function17_group4();

tl::expected<int, my_error_t> fallible_function16_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function17_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function18_group4();

tl::expected<int, my_error_t> fallible_function17_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function18_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function19_group4();

tl::expected<int, my_error_t> fallible_function18_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function19_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function20_group4();

tl::expected<int, my_error_t> fallible_function19_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function20_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function21_group4();

tl::expected<int, my_error_t> fallible_function20_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function21_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function22_group4();

tl::expected<int, my_error_t> fallible_function21_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function22_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function23_group4();

tl::expected<int, my_error_t> fallible_function22_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function23_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function24_group4();

tl::expected<int, my_error_t> fallible_function23_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function24_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function25_group4();

tl::expected<int, my_error_t> fallible_function24_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function25_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function26_group4();

tl::expected<int, my_error_t> fallible_function25_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function26_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function27_group4();

tl::expected<int, my_error_t> fallible_function26_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function27_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function28_group4();

tl::expected<int, my_error_t> fallible_function27_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function28_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function29_group4();

tl::expected<int, my_error_t> fallible_function28_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function29_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function30_group4();

tl::expected<int, my_error_t> fallible_function29_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function30_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function31_group4();

tl::expected<int, my_error_t> fallible_function30_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function31_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function32_group4();

tl::expected<int, my_error_t> fallible_function31_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function32_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function33_group4();

tl::expected<int, my_error_t> fallible_function32_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function33_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function34_group4();

tl::expected<int, my_error_t> fallible_function33_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function34_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function35_group4();

tl::expected<int, my_error_t> fallible_function34_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function35_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function36_group4();

tl::expected<int, my_error_t> fallible_function35_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function36_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function37_group4();

tl::expected<int, my_error_t> fallible_function36_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function37_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function38_group4();

tl::expected<int, my_error_t> fallible_function37_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function38_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function39_group4();

tl::expected<int, my_error_t> fallible_function38_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function39_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function40_group4();

tl::expected<int, my_error_t> fallible_function39_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function40_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function41_group4();

tl::expected<int, my_error_t> fallible_function40_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function41_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function42_group4();

tl::expected<int, my_error_t> fallible_function41_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function42_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function43_group4();

tl::expected<int, my_error_t> fallible_function42_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function43_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function44_group4();

tl::expected<int, my_error_t> fallible_function43_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function44_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function45_group4();

tl::expected<int, my_error_t> fallible_function44_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function45_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function46_group4();

tl::expected<int, my_error_t> fallible_function45_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function46_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function47_group4();

tl::expected<int, my_error_t> fallible_function46_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function47_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function48_group4();

tl::expected<int, my_error_t> fallible_function47_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function48_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function49_group4();

tl::expected<int, my_error_t> fallible_function48_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function49_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function50_group4();

tl::expected<int, my_error_t> fallible_function49_group4()
{
  auto result_a = non_trivial_destructor::initialize(4);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(4);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function50_group4(); result) {
    side_effect4 = side_effect4 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4;
}

tl::expected<int, my_error_t> fallible_function50_group4()
{
  if (auto result = non_trivial_destructor::initialize(50); result) {
    auto result_internal = result.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect4 + 1;
}

tl::expected<int, my_error_t> fallible_function1_group5();

tl::expected<int, my_error_t> fallible_function0_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function1_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function2_group5();

tl::expected<int, my_error_t> fallible_function1_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function2_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function3_group5();

tl::expected<int, my_error_t> fallible_function2_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function3_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function4_group5();

tl::expected<int, my_error_t> fallible_function3_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function4_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function5_group5();

tl::expected<int, my_error_t> fallible_function4_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function5_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function6_group5();

tl::expected<int, my_error_t> fallible_function5_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function6_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function7_group5();

tl::expected<int, my_error_t> fallible_function6_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function7_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function8_group5();

tl::expected<int, my_error_t> fallible_function7_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function8_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function9_group5();

tl::expected<int, my_error_t> fallible_function8_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function9_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function10_group5();

tl::expected<int, my_error_t> fallible_function9_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function10_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function11_group5();

tl::expected<int, my_error_t> fallible_function10_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function11_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function12_group5();

tl::expected<int, my_error_t> fallible_function11_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function12_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function13_group5();

tl::expected<int, my_error_t> fallible_function12_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function13_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function14_group5();

tl::expected<int, my_error_t> fallible_function13_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function14_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function15_group5();

tl::expected<int, my_error_t> fallible_function14_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function15_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function16_group5();

tl::expected<int, my_error_t> fallible_function15_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function16_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function17_group5();

tl::expected<int, my_error_t> fallible_function16_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function17_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function18_group5();

tl::expected<int, my_error_t> fallible_function17_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function18_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function19_group5();

tl::expected<int, my_error_t> fallible_function18_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function19_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function20_group5();

tl::expected<int, my_error_t> fallible_function19_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function20_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function21_group5();

tl::expected<int, my_error_t> fallible_function20_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function21_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function22_group5();

tl::expected<int, my_error_t> fallible_function21_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function22_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function23_group5();

tl::expected<int, my_error_t> fallible_function22_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function23_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function24_group5();

tl::expected<int, my_error_t> fallible_function23_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function24_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function25_group5();

tl::expected<int, my_error_t> fallible_function24_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function25_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function26_group5();

tl::expected<int, my_error_t> fallible_function25_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function26_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function27_group5();

tl::expected<int, my_error_t> fallible_function26_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function27_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function28_group5();

tl::expected<int, my_error_t> fallible_function27_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function28_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function29_group5();

tl::expected<int, my_error_t> fallible_function28_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function29_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function30_group5();

tl::expected<int, my_error_t> fallible_function29_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function30_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function31_group5();

tl::expected<int, my_error_t> fallible_function30_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function31_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function32_group5();

tl::expected<int, my_error_t> fallible_function31_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function32_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function33_group5();

tl::expected<int, my_error_t> fallible_function32_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function33_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function34_group5();

tl::expected<int, my_error_t> fallible_function33_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function34_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function35_group5();

tl::expected<int, my_error_t> fallible_function34_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function35_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function36_group5();

tl::expected<int, my_error_t> fallible_function35_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function36_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function37_group5();

tl::expected<int, my_error_t> fallible_function36_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function37_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function38_group5();

tl::expected<int, my_error_t> fallible_function37_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function38_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function39_group5();

tl::expected<int, my_error_t> fallible_function38_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function39_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function40_group5();

tl::expected<int, my_error_t> fallible_function39_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function40_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function41_group5();

tl::expected<int, my_error_t> fallible_function40_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function41_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function42_group5();

tl::expected<int, my_error_t> fallible_function41_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function42_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function43_group5();

tl::expected<int, my_error_t> fallible_function42_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function43_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function44_group5();

tl::expected<int, my_error_t> fallible_function43_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function44_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function45_group5();

tl::expected<int, my_error_t> fallible_function44_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function45_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function46_group5();

tl::expected<int, my_error_t> fallible_function45_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function46_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function47_group5();

tl::expected<int, my_error_t> fallible_function46_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function47_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function48_group5();

tl::expected<int, my_error_t> fallible_function47_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function48_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function49_group5();

tl::expected<int, my_error_t> fallible_function48_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function49_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function50_group5();

tl::expected<int, my_error_t> fallible_function49_group5()
{
  auto result_a = non_trivial_destructor::initialize(5);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(5);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function50_group5(); result) {
    side_effect5 = side_effect5 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5;
}

tl::expected<int, my_error_t> fallible_function50_group5()
{
  if (auto result = non_trivial_destructor::initialize(50); result) {
    auto result_internal = result.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect5 + 1;
}

tl::expected<int, my_error_t> fallible_function1_group6();

tl::expected<int, my_error_t> fallible_function0_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function1_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function2_group6();

tl::expected<int, my_error_t> fallible_function1_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function2_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function3_group6();

tl::expected<int, my_error_t> fallible_function2_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function3_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function4_group6();

tl::expected<int, my_error_t> fallible_function3_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function4_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function5_group6();

tl::expected<int, my_error_t> fallible_function4_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function5_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function6_group6();

tl::expected<int, my_error_t> fallible_function5_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function6_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function7_group6();

tl::expected<int, my_error_t> fallible_function6_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function7_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function8_group6();

tl::expected<int, my_error_t> fallible_function7_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function8_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function9_group6();

tl::expected<int, my_error_t> fallible_function8_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function9_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function10_group6();

tl::expected<int, my_error_t> fallible_function9_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function10_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function11_group6();

tl::expected<int, my_error_t> fallible_function10_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function11_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function12_group6();

tl::expected<int, my_error_t> fallible_function11_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function12_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function13_group6();

tl::expected<int, my_error_t> fallible_function12_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function13_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function14_group6();

tl::expected<int, my_error_t> fallible_function13_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function14_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function15_group6();

tl::expected<int, my_error_t> fallible_function14_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function15_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function16_group6();

tl::expected<int, my_error_t> fallible_function15_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function16_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function17_group6();

tl::expected<int, my_error_t> fallible_function16_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function17_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function18_group6();

tl::expected<int, my_error_t> fallible_function17_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function18_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function19_group6();

tl::expected<int, my_error_t> fallible_function18_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function19_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function20_group6();

tl::expected<int, my_error_t> fallible_function19_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function20_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function21_group6();

tl::expected<int, my_error_t> fallible_function20_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function21_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function22_group6();

tl::expected<int, my_error_t> fallible_function21_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function22_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function23_group6();

tl::expected<int, my_error_t> fallible_function22_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function23_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function24_group6();

tl::expected<int, my_error_t> fallible_function23_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function24_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function25_group6();

tl::expected<int, my_error_t> fallible_function24_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function25_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function26_group6();

tl::expected<int, my_error_t> fallible_function25_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function26_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function27_group6();

tl::expected<int, my_error_t> fallible_function26_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function27_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function28_group6();

tl::expected<int, my_error_t> fallible_function27_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function28_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function29_group6();

tl::expected<int, my_error_t> fallible_function28_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function29_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function30_group6();

tl::expected<int, my_error_t> fallible_function29_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function30_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function31_group6();

tl::expected<int, my_error_t> fallible_function30_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function31_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function32_group6();

tl::expected<int, my_error_t> fallible_function31_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function32_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function33_group6();

tl::expected<int, my_error_t> fallible_function32_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function33_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function34_group6();

tl::expected<int, my_error_t> fallible_function33_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function34_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function35_group6();

tl::expected<int, my_error_t> fallible_function34_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function35_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function36_group6();

tl::expected<int, my_error_t> fallible_function35_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function36_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function37_group6();

tl::expected<int, my_error_t> fallible_function36_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function37_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function38_group6();

tl::expected<int, my_error_t> fallible_function37_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function38_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function39_group6();

tl::expected<int, my_error_t> fallible_function38_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function39_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function40_group6();

tl::expected<int, my_error_t> fallible_function39_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function40_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function41_group6();

tl::expected<int, my_error_t> fallible_function40_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function41_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function42_group6();

tl::expected<int, my_error_t> fallible_function41_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function42_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function43_group6();

tl::expected<int, my_error_t> fallible_function42_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function43_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function44_group6();

tl::expected<int, my_error_t> fallible_function43_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function44_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function45_group6();

tl::expected<int, my_error_t> fallible_function44_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function45_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function46_group6();

tl::expected<int, my_error_t> fallible_function45_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function46_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function47_group6();

tl::expected<int, my_error_t> fallible_function46_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function47_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function48_group6();

tl::expected<int, my_error_t> fallible_function47_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function48_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function49_group6();

tl::expected<int, my_error_t> fallible_function48_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function49_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function50_group6();

tl::expected<int, my_error_t> fallible_function49_group6()
{
  auto result_a = non_trivial_destructor::initialize(6);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(6);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function50_group6(); result) {
    side_effect6 = side_effect6 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6;
}

tl::expected<int, my_error_t> fallible_function50_group6()
{
  if (auto result = non_trivial_destructor::initialize(50); result) {
    auto result_internal = result.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect6 + 1;
}

tl::expected<int, my_error_t> fallible_function1_group7();

tl::expected<int, my_error_t> fallible_function0_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function1_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function2_group7();

tl::expected<int, my_error_t> fallible_function1_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function2_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function3_group7();

tl::expected<int, my_error_t> fallible_function2_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function3_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function4_group7();

tl::expected<int, my_error_t> fallible_function3_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function4_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function5_group7();

tl::expected<int, my_error_t> fallible_function4_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function5_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function6_group7();

tl::expected<int, my_error_t> fallible_function5_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function6_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function7_group7();

tl::expected<int, my_error_t> fallible_function6_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function7_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function8_group7();

tl::expected<int, my_error_t> fallible_function7_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function8_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function9_group7();

tl::expected<int, my_error_t> fallible_function8_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function9_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function10_group7();

tl::expected<int, my_error_t> fallible_function9_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function10_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function11_group7();

tl::expected<int, my_error_t> fallible_function10_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function11_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function12_group7();

tl::expected<int, my_error_t> fallible_function11_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function12_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function13_group7();

tl::expected<int, my_error_t> fallible_function12_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function13_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function14_group7();

tl::expected<int, my_error_t> fallible_function13_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function14_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function15_group7();

tl::expected<int, my_error_t> fallible_function14_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function15_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function16_group7();

tl::expected<int, my_error_t> fallible_function15_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function16_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function17_group7();

tl::expected<int, my_error_t> fallible_function16_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function17_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function18_group7();

tl::expected<int, my_error_t> fallible_function17_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function18_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function19_group7();

tl::expected<int, my_error_t> fallible_function18_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function19_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function20_group7();

tl::expected<int, my_error_t> fallible_function19_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function20_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function21_group7();

tl::expected<int, my_error_t> fallible_function20_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function21_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function22_group7();

tl::expected<int, my_error_t> fallible_function21_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function22_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function23_group7();

tl::expected<int, my_error_t> fallible_function22_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function23_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function24_group7();

tl::expected<int, my_error_t> fallible_function23_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function24_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function25_group7();

tl::expected<int, my_error_t> fallible_function24_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function25_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function26_group7();

tl::expected<int, my_error_t> fallible_function25_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function26_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function27_group7();

tl::expected<int, my_error_t> fallible_function26_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function27_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function28_group7();

tl::expected<int, my_error_t> fallible_function27_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function28_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function29_group7();

tl::expected<int, my_error_t> fallible_function28_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function29_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function30_group7();

tl::expected<int, my_error_t> fallible_function29_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function30_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function31_group7();

tl::expected<int, my_error_t> fallible_function30_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function31_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function32_group7();

tl::expected<int, my_error_t> fallible_function31_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function32_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function33_group7();

tl::expected<int, my_error_t> fallible_function32_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function33_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function34_group7();

tl::expected<int, my_error_t> fallible_function33_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function34_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function35_group7();

tl::expected<int, my_error_t> fallible_function34_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function35_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function36_group7();

tl::expected<int, my_error_t> fallible_function35_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function36_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function37_group7();

tl::expected<int, my_error_t> fallible_function36_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function37_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function38_group7();

tl::expected<int, my_error_t> fallible_function37_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function38_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function39_group7();

tl::expected<int, my_error_t> fallible_function38_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function39_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function40_group7();

tl::expected<int, my_error_t> fallible_function39_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function40_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function41_group7();

tl::expected<int, my_error_t> fallible_function40_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function41_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function42_group7();

tl::expected<int, my_error_t> fallible_function41_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function42_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function43_group7();

tl::expected<int, my_error_t> fallible_function42_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function43_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function44_group7();

tl::expected<int, my_error_t> fallible_function43_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function44_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function45_group7();

tl::expected<int, my_error_t> fallible_function44_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function45_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function46_group7();

tl::expected<int, my_error_t> fallible_function45_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function46_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function47_group7();

tl::expected<int, my_error_t> fallible_function46_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function47_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function48_group7();

tl::expected<int, my_error_t> fallible_function47_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function48_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function49_group7();

tl::expected<int, my_error_t> fallible_function48_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function49_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function50_group7();

tl::expected<int, my_error_t> fallible_function49_group7()
{
  auto result_a = non_trivial_destructor::initialize(7);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(7);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function50_group7(); result) {
    side_effect7 = side_effect7 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7;
}

tl::expected<int, my_error_t> fallible_function50_group7()
{
  if (auto result = non_trivial_destructor::initialize(50); result) {
    auto result_internal = result.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect7 + 1;
}

tl::expected<int, my_error_t> fallible_function1_group8();

tl::expected<int, my_error_t> fallible_function0_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function1_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function2_group8();

tl::expected<int, my_error_t> fallible_function1_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function2_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function3_group8();

tl::expected<int, my_error_t> fallible_function2_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function3_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function4_group8();

tl::expected<int, my_error_t> fallible_function3_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function4_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function5_group8();

tl::expected<int, my_error_t> fallible_function4_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function5_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function6_group8();

tl::expected<int, my_error_t> fallible_function5_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function6_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function7_group8();

tl::expected<int, my_error_t> fallible_function6_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function7_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function8_group8();

tl::expected<int, my_error_t> fallible_function7_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function8_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function9_group8();

tl::expected<int, my_error_t> fallible_function8_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function9_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function10_group8();

tl::expected<int, my_error_t> fallible_function9_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function10_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function11_group8();

tl::expected<int, my_error_t> fallible_function10_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function11_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function12_group8();

tl::expected<int, my_error_t> fallible_function11_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function12_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function13_group8();

tl::expected<int, my_error_t> fallible_function12_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function13_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function14_group8();

tl::expected<int, my_error_t> fallible_function13_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function14_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function15_group8();

tl::expected<int, my_error_t> fallible_function14_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function15_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function16_group8();

tl::expected<int, my_error_t> fallible_function15_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function16_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function17_group8();

tl::expected<int, my_error_t> fallible_function16_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function17_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function18_group8();

tl::expected<int, my_error_t> fallible_function17_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function18_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function19_group8();

tl::expected<int, my_error_t> fallible_function18_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function19_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function20_group8();

tl::expected<int, my_error_t> fallible_function19_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function20_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function21_group8();

tl::expected<int, my_error_t> fallible_function20_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function21_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function22_group8();

tl::expected<int, my_error_t> fallible_function21_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function22_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function23_group8();

tl::expected<int, my_error_t> fallible_function22_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function23_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function24_group8();

tl::expected<int, my_error_t> fallible_function23_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function24_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function25_group8();

tl::expected<int, my_error_t> fallible_function24_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function25_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function26_group8();

tl::expected<int, my_error_t> fallible_function25_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function26_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function27_group8();

tl::expected<int, my_error_t> fallible_function26_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function27_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function28_group8();

tl::expected<int, my_error_t> fallible_function27_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function28_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function29_group8();

tl::expected<int, my_error_t> fallible_function28_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function29_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function30_group8();

tl::expected<int, my_error_t> fallible_function29_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function30_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function31_group8();

tl::expected<int, my_error_t> fallible_function30_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function31_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function32_group8();

tl::expected<int, my_error_t> fallible_function31_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function32_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function33_group8();

tl::expected<int, my_error_t> fallible_function32_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function33_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function34_group8();

tl::expected<int, my_error_t> fallible_function33_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function34_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function35_group8();

tl::expected<int, my_error_t> fallible_function34_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function35_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function36_group8();

tl::expected<int, my_error_t> fallible_function35_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function36_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function37_group8();

tl::expected<int, my_error_t> fallible_function36_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function37_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function38_group8();

tl::expected<int, my_error_t> fallible_function37_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function38_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function39_group8();

tl::expected<int, my_error_t> fallible_function38_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function39_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function40_group8();

tl::expected<int, my_error_t> fallible_function39_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function40_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function41_group8();

tl::expected<int, my_error_t> fallible_function40_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function41_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function42_group8();

tl::expected<int, my_error_t> fallible_function41_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function42_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function43_group8();

tl::expected<int, my_error_t> fallible_function42_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function43_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function44_group8();

tl::expected<int, my_error_t> fallible_function43_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function44_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function45_group8();

tl::expected<int, my_error_t> fallible_function44_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function45_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function46_group8();

tl::expected<int, my_error_t> fallible_function45_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function46_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function47_group8();

tl::expected<int, my_error_t> fallible_function46_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function47_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function48_group8();

tl::expected<int, my_error_t> fallible_function47_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function48_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function49_group8();

tl::expected<int, my_error_t> fallible_function48_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function49_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function50_group8();

tl::expected<int, my_error_t> fallible_function49_group8()
{
  auto result_a = non_trivial_destructor::initialize(8);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(8);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function50_group8(); result) {
    side_effect8 = side_effect8 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8;
}

tl::expected<int, my_error_t> fallible_function50_group8()
{
  if (auto result = non_trivial_destructor::initialize(50); result) {
    auto result_internal = result.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect8 + 1;
}

tl::expected<int, my_error_t> fallible_function1_group9();

tl::expected<int, my_error_t> fallible_function0_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function1_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function2_group9();

tl::expected<int, my_error_t> fallible_function1_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function2_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function3_group9();

tl::expected<int, my_error_t> fallible_function2_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function3_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function4_group9();

tl::expected<int, my_error_t> fallible_function3_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function4_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function5_group9();

tl::expected<int, my_error_t> fallible_function4_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function5_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function6_group9();

tl::expected<int, my_error_t> fallible_function5_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function6_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function7_group9();

tl::expected<int, my_error_t> fallible_function6_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function7_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function8_group9();

tl::expected<int, my_error_t> fallible_function7_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function8_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function9_group9();

tl::expected<int, my_error_t> fallible_function8_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function9_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function10_group9();

tl::expected<int, my_error_t> fallible_function9_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function10_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function11_group9();

tl::expected<int, my_error_t> fallible_function10_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function11_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function12_group9();

tl::expected<int, my_error_t> fallible_function11_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function12_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function13_group9();

tl::expected<int, my_error_t> fallible_function12_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function13_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function14_group9();

tl::expected<int, my_error_t> fallible_function13_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function14_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function15_group9();

tl::expected<int, my_error_t> fallible_function14_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function15_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function16_group9();

tl::expected<int, my_error_t> fallible_function15_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function16_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function17_group9();

tl::expected<int, my_error_t> fallible_function16_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function17_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function18_group9();

tl::expected<int, my_error_t> fallible_function17_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function18_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function19_group9();

tl::expected<int, my_error_t> fallible_function18_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function19_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function20_group9();

tl::expected<int, my_error_t> fallible_function19_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function20_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function21_group9();

tl::expected<int, my_error_t> fallible_function20_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function21_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function22_group9();

tl::expected<int, my_error_t> fallible_function21_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function22_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function23_group9();

tl::expected<int, my_error_t> fallible_function22_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function23_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function24_group9();

tl::expected<int, my_error_t> fallible_function23_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function24_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function25_group9();

tl::expected<int, my_error_t> fallible_function24_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function25_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function26_group9();

tl::expected<int, my_error_t> fallible_function25_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function26_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function27_group9();

tl::expected<int, my_error_t> fallible_function26_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function27_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function28_group9();

tl::expected<int, my_error_t> fallible_function27_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function28_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function29_group9();

tl::expected<int, my_error_t> fallible_function28_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function29_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function30_group9();

tl::expected<int, my_error_t> fallible_function29_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function30_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function31_group9();

tl::expected<int, my_error_t> fallible_function30_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function31_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function32_group9();

tl::expected<int, my_error_t> fallible_function31_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function32_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function33_group9();

tl::expected<int, my_error_t> fallible_function32_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function33_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function34_group9();

tl::expected<int, my_error_t> fallible_function33_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function34_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function35_group9();

tl::expected<int, my_error_t> fallible_function34_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function35_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function36_group9();

tl::expected<int, my_error_t> fallible_function35_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function36_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function37_group9();

tl::expected<int, my_error_t> fallible_function36_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function37_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function38_group9();

tl::expected<int, my_error_t> fallible_function37_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function38_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function39_group9();

tl::expected<int, my_error_t> fallible_function38_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function39_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function40_group9();

tl::expected<int, my_error_t> fallible_function39_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function40_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function41_group9();

tl::expected<int, my_error_t> fallible_function40_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function41_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function42_group9();

tl::expected<int, my_error_t> fallible_function41_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function42_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function43_group9();

tl::expected<int, my_error_t> fallible_function42_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function43_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function44_group9();

tl::expected<int, my_error_t> fallible_function43_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function44_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function45_group9();

tl::expected<int, my_error_t> fallible_function44_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function45_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function46_group9();

tl::expected<int, my_error_t> fallible_function45_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function46_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function47_group9();

tl::expected<int, my_error_t> fallible_function46_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function47_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function48_group9();

tl::expected<int, my_error_t> fallible_function47_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function48_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function49_group9();

tl::expected<int, my_error_t> fallible_function48_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function49_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function50_group9();

tl::expected<int, my_error_t> fallible_function49_group9()
{
  auto result_a = non_trivial_destructor::initialize(9);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(9);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function50_group9(); result) {
    side_effect9 = side_effect9 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9;
}

tl::expected<int, my_error_t> fallible_function50_group9()
{
  if (auto result = non_trivial_destructor::initialize(50); result) {
    auto result_internal = result.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect9 + 1;
}

tl::expected<int, my_error_t> fallible_function1_group10();

tl::expected<int, my_error_t> fallible_function0_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function1_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function2_group10();

tl::expected<int, my_error_t> fallible_function1_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function2_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function3_group10();

tl::expected<int, my_error_t> fallible_function2_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function3_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function4_group10();

tl::expected<int, my_error_t> fallible_function3_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function4_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function5_group10();

tl::expected<int, my_error_t> fallible_function4_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function5_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function6_group10();

tl::expected<int, my_error_t> fallible_function5_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function6_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function7_group10();

tl::expected<int, my_error_t> fallible_function6_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function7_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function8_group10();

tl::expected<int, my_error_t> fallible_function7_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function8_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function9_group10();

tl::expected<int, my_error_t> fallible_function8_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function9_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function10_group10();

tl::expected<int, my_error_t> fallible_function9_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function10_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function11_group10();

tl::expected<int, my_error_t> fallible_function10_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function11_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function12_group10();

tl::expected<int, my_error_t> fallible_function11_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function12_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function13_group10();

tl::expected<int, my_error_t> fallible_function12_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function13_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function14_group10();

tl::expected<int, my_error_t> fallible_function13_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function14_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function15_group10();

tl::expected<int, my_error_t> fallible_function14_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function15_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function16_group10();

tl::expected<int, my_error_t> fallible_function15_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function16_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function17_group10();

tl::expected<int, my_error_t> fallible_function16_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function17_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function18_group10();

tl::expected<int, my_error_t> fallible_function17_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function18_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function19_group10();

tl::expected<int, my_error_t> fallible_function18_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function19_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function20_group10();

tl::expected<int, my_error_t> fallible_function19_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function20_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function21_group10();

tl::expected<int, my_error_t> fallible_function20_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function21_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function22_group10();

tl::expected<int, my_error_t> fallible_function21_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function22_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function23_group10();

tl::expected<int, my_error_t> fallible_function22_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function23_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function24_group10();

tl::expected<int, my_error_t> fallible_function23_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function24_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function25_group10();

tl::expected<int, my_error_t> fallible_function24_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function25_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function26_group10();

tl::expected<int, my_error_t> fallible_function25_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function26_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function27_group10();

tl::expected<int, my_error_t> fallible_function26_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function27_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function28_group10();

tl::expected<int, my_error_t> fallible_function27_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function28_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function29_group10();

tl::expected<int, my_error_t> fallible_function28_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function29_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function30_group10();

tl::expected<int, my_error_t> fallible_function29_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function30_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function31_group10();

tl::expected<int, my_error_t> fallible_function30_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function31_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function32_group10();

tl::expected<int, my_error_t> fallible_function31_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function32_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function33_group10();

tl::expected<int, my_error_t> fallible_function32_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function33_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function34_group10();

tl::expected<int, my_error_t> fallible_function33_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function34_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function35_group10();

tl::expected<int, my_error_t> fallible_function34_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function35_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function36_group10();

tl::expected<int, my_error_t> fallible_function35_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function36_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function37_group10();

tl::expected<int, my_error_t> fallible_function36_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function37_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function38_group10();

tl::expected<int, my_error_t> fallible_function37_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function38_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function39_group10();

tl::expected<int, my_error_t> fallible_function38_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function39_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function40_group10();

tl::expected<int, my_error_t> fallible_function39_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function40_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function41_group10();

tl::expected<int, my_error_t> fallible_function40_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function41_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function42_group10();

tl::expected<int, my_error_t> fallible_function41_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function42_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function43_group10();

tl::expected<int, my_error_t> fallible_function42_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function43_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function44_group10();

tl::expected<int, my_error_t> fallible_function43_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function44_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function45_group10();

tl::expected<int, my_error_t> fallible_function44_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function45_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function46_group10();

tl::expected<int, my_error_t> fallible_function45_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function46_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function47_group10();

tl::expected<int, my_error_t> fallible_function46_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function47_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function48_group10();

tl::expected<int, my_error_t> fallible_function47_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function48_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function49_group10();

tl::expected<int, my_error_t> fallible_function48_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function49_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function50_group10();

tl::expected<int, my_error_t> fallible_function49_group10()
{
  auto result_a = non_trivial_destructor::initialize(10);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(10);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function50_group10(); result) {
    side_effect10 = side_effect10 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10;
}

tl::expected<int, my_error_t> fallible_function50_group10()
{
  if (auto result = non_trivial_destructor::initialize(50); result) {
    auto result_internal = result.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect10 + 1;
}

tl::expected<int, my_error_t> fallible_function1_group11();

tl::expected<int, my_error_t> fallible_function0_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function1_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function2_group11();

tl::expected<int, my_error_t> fallible_function1_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function2_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function3_group11();

tl::expected<int, my_error_t> fallible_function2_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function3_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function4_group11();

tl::expected<int, my_error_t> fallible_function3_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function4_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function5_group11();

tl::expected<int, my_error_t> fallible_function4_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function5_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function6_group11();

tl::expected<int, my_error_t> fallible_function5_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function6_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function7_group11();

tl::expected<int, my_error_t> fallible_function6_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function7_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function8_group11();

tl::expected<int, my_error_t> fallible_function7_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function8_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function9_group11();

tl::expected<int, my_error_t> fallible_function8_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function9_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function10_group11();

tl::expected<int, my_error_t> fallible_function9_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function10_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function11_group11();

tl::expected<int, my_error_t> fallible_function10_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function11_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function12_group11();

tl::expected<int, my_error_t> fallible_function11_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function12_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function13_group11();

tl::expected<int, my_error_t> fallible_function12_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function13_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function14_group11();

tl::expected<int, my_error_t> fallible_function13_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function14_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function15_group11();

tl::expected<int, my_error_t> fallible_function14_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function15_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function16_group11();

tl::expected<int, my_error_t> fallible_function15_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function16_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function17_group11();

tl::expected<int, my_error_t> fallible_function16_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function17_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function18_group11();

tl::expected<int, my_error_t> fallible_function17_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function18_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function19_group11();

tl::expected<int, my_error_t> fallible_function18_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function19_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function20_group11();

tl::expected<int, my_error_t> fallible_function19_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function20_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function21_group11();

tl::expected<int, my_error_t> fallible_function20_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function21_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function22_group11();

tl::expected<int, my_error_t> fallible_function21_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function22_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function23_group11();

tl::expected<int, my_error_t> fallible_function22_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function23_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function24_group11();

tl::expected<int, my_error_t> fallible_function23_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function24_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function25_group11();

tl::expected<int, my_error_t> fallible_function24_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function25_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function26_group11();

tl::expected<int, my_error_t> fallible_function25_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function26_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function27_group11();

tl::expected<int, my_error_t> fallible_function26_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function27_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function28_group11();

tl::expected<int, my_error_t> fallible_function27_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function28_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function29_group11();

tl::expected<int, my_error_t> fallible_function28_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function29_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function30_group11();

tl::expected<int, my_error_t> fallible_function29_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function30_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function31_group11();

tl::expected<int, my_error_t> fallible_function30_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function31_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function32_group11();

tl::expected<int, my_error_t> fallible_function31_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function32_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function33_group11();

tl::expected<int, my_error_t> fallible_function32_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function33_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function34_group11();

tl::expected<int, my_error_t> fallible_function33_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function34_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function35_group11();

tl::expected<int, my_error_t> fallible_function34_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function35_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function36_group11();

tl::expected<int, my_error_t> fallible_function35_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function36_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function37_group11();

tl::expected<int, my_error_t> fallible_function36_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function37_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function38_group11();

tl::expected<int, my_error_t> fallible_function37_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function38_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function39_group11();

tl::expected<int, my_error_t> fallible_function38_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function39_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function40_group11();

tl::expected<int, my_error_t> fallible_function39_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function40_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function41_group11();

tl::expected<int, my_error_t> fallible_function40_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function41_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function42_group11();

tl::expected<int, my_error_t> fallible_function41_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function42_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function43_group11();

tl::expected<int, my_error_t> fallible_function42_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function43_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function44_group11();

tl::expected<int, my_error_t> fallible_function43_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function44_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function45_group11();

tl::expected<int, my_error_t> fallible_function44_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function45_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function46_group11();

tl::expected<int, my_error_t> fallible_function45_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function46_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function47_group11();

tl::expected<int, my_error_t> fallible_function46_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function47_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function48_group11();

tl::expected<int, my_error_t> fallible_function47_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function48_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function49_group11();

tl::expected<int, my_error_t> fallible_function48_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function49_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function50_group11();

tl::expected<int, my_error_t> fallible_function49_group11()
{
  auto result_a = non_trivial_destructor::initialize(11);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(11);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function50_group11(); result) {
    side_effect11 = side_effect11 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11;
}

tl::expected<int, my_error_t> fallible_function50_group11()
{
  if (auto result = non_trivial_destructor::initialize(50); result) {
    auto result_internal = result.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect11 + 1;
}

tl::expected<int, my_error_t> fallible_function1_group12();

tl::expected<int, my_error_t> fallible_function0_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function1_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function2_group12();

tl::expected<int, my_error_t> fallible_function1_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function2_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function3_group12();

tl::expected<int, my_error_t> fallible_function2_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function3_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function4_group12();

tl::expected<int, my_error_t> fallible_function3_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function4_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function5_group12();

tl::expected<int, my_error_t> fallible_function4_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function5_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function6_group12();

tl::expected<int, my_error_t> fallible_function5_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function6_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function7_group12();

tl::expected<int, my_error_t> fallible_function6_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function7_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function8_group12();

tl::expected<int, my_error_t> fallible_function7_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function8_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function9_group12();

tl::expected<int, my_error_t> fallible_function8_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function9_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function10_group12();

tl::expected<int, my_error_t> fallible_function9_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function10_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function11_group12();

tl::expected<int, my_error_t> fallible_function10_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function11_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function12_group12();

tl::expected<int, my_error_t> fallible_function11_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function12_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function13_group12();

tl::expected<int, my_error_t> fallible_function12_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function13_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function14_group12();

tl::expected<int, my_error_t> fallible_function13_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function14_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function15_group12();

tl::expected<int, my_error_t> fallible_function14_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function15_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function16_group12();

tl::expected<int, my_error_t> fallible_function15_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function16_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function17_group12();

tl::expected<int, my_error_t> fallible_function16_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function17_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function18_group12();

tl::expected<int, my_error_t> fallible_function17_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function18_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function19_group12();

tl::expected<int, my_error_t> fallible_function18_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function19_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function20_group12();

tl::expected<int, my_error_t> fallible_function19_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function20_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function21_group12();

tl::expected<int, my_error_t> fallible_function20_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function21_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function22_group12();

tl::expected<int, my_error_t> fallible_function21_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function22_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function23_group12();

tl::expected<int, my_error_t> fallible_function22_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function23_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function24_group12();

tl::expected<int, my_error_t> fallible_function23_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function24_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function25_group12();

tl::expected<int, my_error_t> fallible_function24_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function25_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function26_group12();

tl::expected<int, my_error_t> fallible_function25_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function26_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function27_group12();

tl::expected<int, my_error_t> fallible_function26_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function27_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function28_group12();

tl::expected<int, my_error_t> fallible_function27_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function28_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function29_group12();

tl::expected<int, my_error_t> fallible_function28_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function29_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function30_group12();

tl::expected<int, my_error_t> fallible_function29_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function30_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function31_group12();

tl::expected<int, my_error_t> fallible_function30_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function31_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function32_group12();

tl::expected<int, my_error_t> fallible_function31_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function32_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function33_group12();

tl::expected<int, my_error_t> fallible_function32_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function33_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function34_group12();

tl::expected<int, my_error_t> fallible_function33_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function34_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function35_group12();

tl::expected<int, my_error_t> fallible_function34_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function35_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function36_group12();

tl::expected<int, my_error_t> fallible_function35_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function36_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function37_group12();

tl::expected<int, my_error_t> fallible_function36_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function37_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function38_group12();

tl::expected<int, my_error_t> fallible_function37_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function38_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function39_group12();

tl::expected<int, my_error_t> fallible_function38_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function39_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function40_group12();

tl::expected<int, my_error_t> fallible_function39_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function40_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function41_group12();

tl::expected<int, my_error_t> fallible_function40_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function41_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function42_group12();

tl::expected<int, my_error_t> fallible_function41_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function42_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function43_group12();

tl::expected<int, my_error_t> fallible_function42_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function43_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function44_group12();

tl::expected<int, my_error_t> fallible_function43_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function44_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function45_group12();

tl::expected<int, my_error_t> fallible_function44_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function45_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function46_group12();

tl::expected<int, my_error_t> fallible_function45_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function46_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function47_group12();

tl::expected<int, my_error_t> fallible_function46_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function47_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function48_group12();

tl::expected<int, my_error_t> fallible_function47_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function48_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function49_group12();

tl::expected<int, my_error_t> fallible_function48_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function49_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function50_group12();

tl::expected<int, my_error_t> fallible_function49_group12()
{
  auto result_a = non_trivial_destructor::initialize(12);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(12);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function50_group12(); result) {
    side_effect12 = side_effect12 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12;
}

tl::expected<int, my_error_t> fallible_function50_group12()
{
  if (auto result = non_trivial_destructor::initialize(50); result) {
    auto result_internal = result.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect12 + 1;
}

tl::expected<int, my_error_t> fallible_function1_group13();

tl::expected<int, my_error_t> fallible_function0_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function1_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function2_group13();

tl::expected<int, my_error_t> fallible_function1_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function2_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function3_group13();

tl::expected<int, my_error_t> fallible_function2_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function3_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function4_group13();

tl::expected<int, my_error_t> fallible_function3_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function4_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function5_group13();

tl::expected<int, my_error_t> fallible_function4_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function5_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function6_group13();

tl::expected<int, my_error_t> fallible_function5_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function6_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function7_group13();

tl::expected<int, my_error_t> fallible_function6_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function7_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function8_group13();

tl::expected<int, my_error_t> fallible_function7_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function8_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function9_group13();

tl::expected<int, my_error_t> fallible_function8_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function9_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function10_group13();

tl::expected<int, my_error_t> fallible_function9_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function10_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function11_group13();

tl::expected<int, my_error_t> fallible_function10_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function11_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function12_group13();

tl::expected<int, my_error_t> fallible_function11_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function12_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function13_group13();

tl::expected<int, my_error_t> fallible_function12_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function13_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function14_group13();

tl::expected<int, my_error_t> fallible_function13_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function14_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function15_group13();

tl::expected<int, my_error_t> fallible_function14_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function15_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function16_group13();

tl::expected<int, my_error_t> fallible_function15_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function16_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function17_group13();

tl::expected<int, my_error_t> fallible_function16_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function17_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function18_group13();

tl::expected<int, my_error_t> fallible_function17_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function18_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function19_group13();

tl::expected<int, my_error_t> fallible_function18_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function19_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function20_group13();

tl::expected<int, my_error_t> fallible_function19_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function20_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function21_group13();

tl::expected<int, my_error_t> fallible_function20_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function21_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function22_group13();

tl::expected<int, my_error_t> fallible_function21_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function22_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function23_group13();

tl::expected<int, my_error_t> fallible_function22_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function23_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function24_group13();

tl::expected<int, my_error_t> fallible_function23_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function24_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function25_group13();

tl::expected<int, my_error_t> fallible_function24_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function25_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function26_group13();

tl::expected<int, my_error_t> fallible_function25_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function26_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function27_group13();

tl::expected<int, my_error_t> fallible_function26_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function27_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function28_group13();

tl::expected<int, my_error_t> fallible_function27_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function28_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function29_group13();

tl::expected<int, my_error_t> fallible_function28_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function29_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function30_group13();

tl::expected<int, my_error_t> fallible_function29_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function30_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function31_group13();

tl::expected<int, my_error_t> fallible_function30_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function31_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function32_group13();

tl::expected<int, my_error_t> fallible_function31_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function32_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function33_group13();

tl::expected<int, my_error_t> fallible_function32_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function33_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function34_group13();

tl::expected<int, my_error_t> fallible_function33_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function34_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function35_group13();

tl::expected<int, my_error_t> fallible_function34_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function35_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function36_group13();

tl::expected<int, my_error_t> fallible_function35_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function36_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function37_group13();

tl::expected<int, my_error_t> fallible_function36_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function37_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function38_group13();

tl::expected<int, my_error_t> fallible_function37_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function38_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function39_group13();

tl::expected<int, my_error_t> fallible_function38_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function39_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function40_group13();

tl::expected<int, my_error_t> fallible_function39_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function40_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function41_group13();

tl::expected<int, my_error_t> fallible_function40_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function41_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function42_group13();

tl::expected<int, my_error_t> fallible_function41_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function42_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function43_group13();

tl::expected<int, my_error_t> fallible_function42_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function43_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function44_group13();

tl::expected<int, my_error_t> fallible_function43_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function44_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function45_group13();

tl::expected<int, my_error_t> fallible_function44_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function45_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function46_group13();

tl::expected<int, my_error_t> fallible_function45_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function46_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function47_group13();

tl::expected<int, my_error_t> fallible_function46_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function47_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function48_group13();

tl::expected<int, my_error_t> fallible_function47_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function48_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function49_group13();

tl::expected<int, my_error_t> fallible_function48_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function49_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function50_group13();

tl::expected<int, my_error_t> fallible_function49_group13()
{
  auto result_a = non_trivial_destructor::initialize(13);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(13);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function50_group13(); result) {
    side_effect13 = side_effect13 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13;
}

tl::expected<int, my_error_t> fallible_function50_group13()
{
  if (auto result = non_trivial_destructor::initialize(50); result) {
    auto result_internal = result.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect13 + 1;
}

tl::expected<int, my_error_t> fallible_function1_group14();

tl::expected<int, my_error_t> fallible_function0_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function1_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function2_group14();

tl::expected<int, my_error_t> fallible_function1_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function2_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function3_group14();

tl::expected<int, my_error_t> fallible_function2_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function3_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function4_group14();

tl::expected<int, my_error_t> fallible_function3_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function4_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function5_group14();

tl::expected<int, my_error_t> fallible_function4_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function5_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function6_group14();

tl::expected<int, my_error_t> fallible_function5_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function6_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function7_group14();

tl::expected<int, my_error_t> fallible_function6_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function7_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function8_group14();

tl::expected<int, my_error_t> fallible_function7_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function8_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function9_group14();

tl::expected<int, my_error_t> fallible_function8_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function9_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function10_group14();

tl::expected<int, my_error_t> fallible_function9_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function10_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function11_group14();

tl::expected<int, my_error_t> fallible_function10_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function11_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function12_group14();

tl::expected<int, my_error_t> fallible_function11_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function12_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function13_group14();

tl::expected<int, my_error_t> fallible_function12_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function13_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function14_group14();

tl::expected<int, my_error_t> fallible_function13_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function14_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function15_group14();

tl::expected<int, my_error_t> fallible_function14_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function15_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function16_group14();

tl::expected<int, my_error_t> fallible_function15_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function16_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function17_group14();

tl::expected<int, my_error_t> fallible_function16_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function17_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function18_group14();

tl::expected<int, my_error_t> fallible_function17_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function18_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function19_group14();

tl::expected<int, my_error_t> fallible_function18_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function19_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function20_group14();

tl::expected<int, my_error_t> fallible_function19_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function20_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function21_group14();

tl::expected<int, my_error_t> fallible_function20_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function21_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function22_group14();

tl::expected<int, my_error_t> fallible_function21_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function22_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function23_group14();

tl::expected<int, my_error_t> fallible_function22_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function23_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function24_group14();

tl::expected<int, my_error_t> fallible_function23_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function24_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function25_group14();

tl::expected<int, my_error_t> fallible_function24_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function25_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function26_group14();

tl::expected<int, my_error_t> fallible_function25_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function26_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function27_group14();

tl::expected<int, my_error_t> fallible_function26_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function27_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function28_group14();

tl::expected<int, my_error_t> fallible_function27_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function28_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function29_group14();

tl::expected<int, my_error_t> fallible_function28_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function29_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function30_group14();

tl::expected<int, my_error_t> fallible_function29_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function30_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function31_group14();

tl::expected<int, my_error_t> fallible_function30_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function31_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function32_group14();

tl::expected<int, my_error_t> fallible_function31_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function32_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function33_group14();

tl::expected<int, my_error_t> fallible_function32_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function33_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function34_group14();

tl::expected<int, my_error_t> fallible_function33_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function34_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function35_group14();

tl::expected<int, my_error_t> fallible_function34_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function35_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function36_group14();

tl::expected<int, my_error_t> fallible_function35_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function36_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function37_group14();

tl::expected<int, my_error_t> fallible_function36_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function37_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function38_group14();

tl::expected<int, my_error_t> fallible_function37_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function38_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function39_group14();

tl::expected<int, my_error_t> fallible_function38_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function39_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function40_group14();

tl::expected<int, my_error_t> fallible_function39_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function40_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function41_group14();

tl::expected<int, my_error_t> fallible_function40_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function41_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function42_group14();

tl::expected<int, my_error_t> fallible_function41_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function42_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function43_group14();

tl::expected<int, my_error_t> fallible_function42_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function43_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function44_group14();

tl::expected<int, my_error_t> fallible_function43_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function44_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function45_group14();

tl::expected<int, my_error_t> fallible_function44_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function45_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function46_group14();

tl::expected<int, my_error_t> fallible_function45_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function46_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function47_group14();

tl::expected<int, my_error_t> fallible_function46_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function47_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function48_group14();

tl::expected<int, my_error_t> fallible_function47_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function48_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function49_group14();

tl::expected<int, my_error_t> fallible_function48_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function49_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function50_group14();

tl::expected<int, my_error_t> fallible_function49_group14()
{
  auto result_a = non_trivial_destructor::initialize(14);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(14);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function50_group14(); result) {
    side_effect14 = side_effect14 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14;
}

tl::expected<int, my_error_t> fallible_function50_group14()
{
  if (auto result = non_trivial_destructor::initialize(50); result) {
    auto result_internal = result.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect14 + 1;
}

tl::expected<int, my_error_t> fallible_function1_group15();

tl::expected<int, my_error_t> fallible_function0_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function1_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function2_group15();

tl::expected<int, my_error_t> fallible_function1_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function2_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function3_group15();

tl::expected<int, my_error_t> fallible_function2_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function3_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function4_group15();

tl::expected<int, my_error_t> fallible_function3_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function4_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function5_group15();

tl::expected<int, my_error_t> fallible_function4_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function5_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function6_group15();

tl::expected<int, my_error_t> fallible_function5_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function6_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function7_group15();

tl::expected<int, my_error_t> fallible_function6_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function7_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function8_group15();

tl::expected<int, my_error_t> fallible_function7_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function8_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function9_group15();

tl::expected<int, my_error_t> fallible_function8_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function9_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function10_group15();

tl::expected<int, my_error_t> fallible_function9_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function10_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function11_group15();

tl::expected<int, my_error_t> fallible_function10_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function11_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function12_group15();

tl::expected<int, my_error_t> fallible_function11_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function12_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function13_group15();

tl::expected<int, my_error_t> fallible_function12_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function13_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function14_group15();

tl::expected<int, my_error_t> fallible_function13_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function14_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function15_group15();

tl::expected<int, my_error_t> fallible_function14_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function15_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function16_group15();

tl::expected<int, my_error_t> fallible_function15_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function16_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function17_group15();

tl::expected<int, my_error_t> fallible_function16_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function17_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function18_group15();

tl::expected<int, my_error_t> fallible_function17_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function18_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function19_group15();

tl::expected<int, my_error_t> fallible_function18_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function19_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function20_group15();

tl::expected<int, my_error_t> fallible_function19_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function20_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function21_group15();

tl::expected<int, my_error_t> fallible_function20_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function21_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function22_group15();

tl::expected<int, my_error_t> fallible_function21_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function22_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function23_group15();

tl::expected<int, my_error_t> fallible_function22_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function23_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function24_group15();

tl::expected<int, my_error_t> fallible_function23_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function24_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function25_group15();

tl::expected<int, my_error_t> fallible_function24_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function25_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function26_group15();

tl::expected<int, my_error_t> fallible_function25_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function26_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function27_group15();

tl::expected<int, my_error_t> fallible_function26_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function27_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function28_group15();

tl::expected<int, my_error_t> fallible_function27_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function28_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function29_group15();

tl::expected<int, my_error_t> fallible_function28_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function29_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function30_group15();

tl::expected<int, my_error_t> fallible_function29_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function30_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function31_group15();

tl::expected<int, my_error_t> fallible_function30_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function31_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function32_group15();

tl::expected<int, my_error_t> fallible_function31_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function32_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function33_group15();

tl::expected<int, my_error_t> fallible_function32_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function33_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function34_group15();

tl::expected<int, my_error_t> fallible_function33_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function34_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function35_group15();

tl::expected<int, my_error_t> fallible_function34_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function35_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function36_group15();

tl::expected<int, my_error_t> fallible_function35_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function36_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function37_group15();

tl::expected<int, my_error_t> fallible_function36_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function37_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function38_group15();

tl::expected<int, my_error_t> fallible_function37_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function38_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function39_group15();

tl::expected<int, my_error_t> fallible_function38_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function39_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function40_group15();

tl::expected<int, my_error_t> fallible_function39_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function40_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function41_group15();

tl::expected<int, my_error_t> fallible_function40_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function41_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function42_group15();

tl::expected<int, my_error_t> fallible_function41_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function42_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function43_group15();

tl::expected<int, my_error_t> fallible_function42_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function43_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function44_group15();

tl::expected<int, my_error_t> fallible_function43_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function44_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function45_group15();

tl::expected<int, my_error_t> fallible_function44_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function45_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function46_group15();

tl::expected<int, my_error_t> fallible_function45_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function46_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function47_group15();

tl::expected<int, my_error_t> fallible_function46_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function47_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function48_group15();

tl::expected<int, my_error_t> fallible_function47_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function48_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function49_group15();

tl::expected<int, my_error_t> fallible_function48_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function49_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function50_group15();

tl::expected<int, my_error_t> fallible_function49_group15()
{
  auto result_a = non_trivial_destructor::initialize(15);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(15);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function50_group15(); result) {
    side_effect15 = side_effect15 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15;
}

tl::expected<int, my_error_t> fallible_function50_group15()
{
  if (auto result = non_trivial_destructor::initialize(50); result) {
    auto result_internal = result.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect15 + 1;
}

tl::expected<int, my_error_t> fallible_function1_group16();

tl::expected<int, my_error_t> fallible_function0_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function1_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function2_group16();

tl::expected<int, my_error_t> fallible_function1_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function2_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function3_group16();

tl::expected<int, my_error_t> fallible_function2_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function3_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function4_group16();

tl::expected<int, my_error_t> fallible_function3_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function4_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function5_group16();

tl::expected<int, my_error_t> fallible_function4_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function5_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function6_group16();

tl::expected<int, my_error_t> fallible_function5_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function6_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function7_group16();

tl::expected<int, my_error_t> fallible_function6_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function7_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function8_group16();

tl::expected<int, my_error_t> fallible_function7_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function8_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function9_group16();

tl::expected<int, my_error_t> fallible_function8_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function9_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function10_group16();

tl::expected<int, my_error_t> fallible_function9_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function10_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function11_group16();

tl::expected<int, my_error_t> fallible_function10_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function11_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function12_group16();

tl::expected<int, my_error_t> fallible_function11_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function12_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function13_group16();

tl::expected<int, my_error_t> fallible_function12_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function13_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function14_group16();

tl::expected<int, my_error_t> fallible_function13_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function14_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function15_group16();

tl::expected<int, my_error_t> fallible_function14_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function15_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function16_group16();

tl::expected<int, my_error_t> fallible_function15_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function16_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function17_group16();

tl::expected<int, my_error_t> fallible_function16_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function17_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function18_group16();

tl::expected<int, my_error_t> fallible_function17_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function18_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function19_group16();

tl::expected<int, my_error_t> fallible_function18_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function19_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function20_group16();

tl::expected<int, my_error_t> fallible_function19_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function20_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function21_group16();

tl::expected<int, my_error_t> fallible_function20_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function21_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function22_group16();

tl::expected<int, my_error_t> fallible_function21_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function22_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function23_group16();

tl::expected<int, my_error_t> fallible_function22_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function23_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function24_group16();

tl::expected<int, my_error_t> fallible_function23_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function24_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function25_group16();

tl::expected<int, my_error_t> fallible_function24_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function25_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function26_group16();

tl::expected<int, my_error_t> fallible_function25_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function26_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function27_group16();

tl::expected<int, my_error_t> fallible_function26_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function27_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function28_group16();

tl::expected<int, my_error_t> fallible_function27_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function28_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function29_group16();

tl::expected<int, my_error_t> fallible_function28_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function29_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function30_group16();

tl::expected<int, my_error_t> fallible_function29_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function30_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function31_group16();

tl::expected<int, my_error_t> fallible_function30_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function31_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function32_group16();

tl::expected<int, my_error_t> fallible_function31_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function32_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function33_group16();

tl::expected<int, my_error_t> fallible_function32_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function33_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function34_group16();

tl::expected<int, my_error_t> fallible_function33_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function34_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function35_group16();

tl::expected<int, my_error_t> fallible_function34_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function35_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function36_group16();

tl::expected<int, my_error_t> fallible_function35_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function36_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function37_group16();

tl::expected<int, my_error_t> fallible_function36_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function37_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function38_group16();

tl::expected<int, my_error_t> fallible_function37_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function38_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function39_group16();

tl::expected<int, my_error_t> fallible_function38_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function39_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function40_group16();

tl::expected<int, my_error_t> fallible_function39_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function40_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function41_group16();

tl::expected<int, my_error_t> fallible_function40_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function41_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function42_group16();

tl::expected<int, my_error_t> fallible_function41_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function42_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function43_group16();

tl::expected<int, my_error_t> fallible_function42_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function43_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function44_group16();

tl::expected<int, my_error_t> fallible_function43_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function44_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function45_group16();

tl::expected<int, my_error_t> fallible_function44_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function45_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function46_group16();

tl::expected<int, my_error_t> fallible_function45_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function46_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function47_group16();

tl::expected<int, my_error_t> fallible_function46_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function47_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function48_group16();

tl::expected<int, my_error_t> fallible_function47_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function48_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function49_group16();

tl::expected<int, my_error_t> fallible_function48_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function49_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function50_group16();

tl::expected<int, my_error_t> fallible_function49_group16()
{
  auto result_a = non_trivial_destructor::initialize(16);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(16);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function50_group16(); result) {
    side_effect16 = side_effect16 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16;
}

tl::expected<int, my_error_t> fallible_function50_group16()
{
  if (auto result = non_trivial_destructor::initialize(50); result) {
    auto result_internal = result.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect16 + 1;
}

tl::expected<int, my_error_t> fallible_function1_group17();

tl::expected<int, my_error_t> fallible_function0_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function1_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function2_group17();

tl::expected<int, my_error_t> fallible_function1_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function2_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function3_group17();

tl::expected<int, my_error_t> fallible_function2_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function3_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function4_group17();

tl::expected<int, my_error_t> fallible_function3_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function4_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function5_group17();

tl::expected<int, my_error_t> fallible_function4_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function5_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function6_group17();

tl::expected<int, my_error_t> fallible_function5_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function6_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function7_group17();

tl::expected<int, my_error_t> fallible_function6_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function7_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function8_group17();

tl::expected<int, my_error_t> fallible_function7_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function8_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function9_group17();

tl::expected<int, my_error_t> fallible_function8_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function9_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function10_group17();

tl::expected<int, my_error_t> fallible_function9_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function10_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function11_group17();

tl::expected<int, my_error_t> fallible_function10_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function11_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function12_group17();

tl::expected<int, my_error_t> fallible_function11_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function12_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function13_group17();

tl::expected<int, my_error_t> fallible_function12_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function13_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function14_group17();

tl::expected<int, my_error_t> fallible_function13_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function14_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function15_group17();

tl::expected<int, my_error_t> fallible_function14_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function15_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function16_group17();

tl::expected<int, my_error_t> fallible_function15_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function16_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function17_group17();

tl::expected<int, my_error_t> fallible_function16_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function17_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function18_group17();

tl::expected<int, my_error_t> fallible_function17_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function18_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function19_group17();

tl::expected<int, my_error_t> fallible_function18_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function19_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function20_group17();

tl::expected<int, my_error_t> fallible_function19_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function20_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function21_group17();

tl::expected<int, my_error_t> fallible_function20_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function21_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function22_group17();

tl::expected<int, my_error_t> fallible_function21_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function22_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function23_group17();

tl::expected<int, my_error_t> fallible_function22_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function23_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function24_group17();

tl::expected<int, my_error_t> fallible_function23_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function24_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function25_group17();

tl::expected<int, my_error_t> fallible_function24_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function25_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function26_group17();

tl::expected<int, my_error_t> fallible_function25_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function26_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function27_group17();

tl::expected<int, my_error_t> fallible_function26_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function27_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function28_group17();

tl::expected<int, my_error_t> fallible_function27_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function28_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function29_group17();

tl::expected<int, my_error_t> fallible_function28_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function29_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function30_group17();

tl::expected<int, my_error_t> fallible_function29_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function30_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function31_group17();

tl::expected<int, my_error_t> fallible_function30_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function31_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function32_group17();

tl::expected<int, my_error_t> fallible_function31_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function32_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function33_group17();

tl::expected<int, my_error_t> fallible_function32_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function33_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function34_group17();

tl::expected<int, my_error_t> fallible_function33_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function34_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function35_group17();

tl::expected<int, my_error_t> fallible_function34_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function35_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function36_group17();

tl::expected<int, my_error_t> fallible_function35_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function36_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function37_group17();

tl::expected<int, my_error_t> fallible_function36_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function37_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function38_group17();

tl::expected<int, my_error_t> fallible_function37_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function38_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function39_group17();

tl::expected<int, my_error_t> fallible_function38_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function39_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function40_group17();

tl::expected<int, my_error_t> fallible_function39_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function40_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function41_group17();

tl::expected<int, my_error_t> fallible_function40_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function41_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function42_group17();

tl::expected<int, my_error_t> fallible_function41_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function42_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function43_group17();

tl::expected<int, my_error_t> fallible_function42_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function43_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function44_group17();

tl::expected<int, my_error_t> fallible_function43_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function44_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function45_group17();

tl::expected<int, my_error_t> fallible_function44_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function45_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function46_group17();

tl::expected<int, my_error_t> fallible_function45_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function46_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function47_group17();

tl::expected<int, my_error_t> fallible_function46_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function47_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function48_group17();

tl::expected<int, my_error_t> fallible_function47_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function48_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function49_group17();

tl::expected<int, my_error_t> fallible_function48_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function49_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function50_group17();

tl::expected<int, my_error_t> fallible_function49_group17()
{
  auto result_a = non_trivial_destructor::initialize(17);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(17);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function50_group17(); result) {
    side_effect17 = side_effect17 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17;
}

tl::expected<int, my_error_t> fallible_function50_group17()
{
  if (auto result = non_trivial_destructor::initialize(50); result) {
    auto result_internal = result.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect17 + 1;
}

tl::expected<int, my_error_t> fallible_function1_group18();

tl::expected<int, my_error_t> fallible_function0_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function1_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function2_group18();

tl::expected<int, my_error_t> fallible_function1_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function2_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function3_group18();

tl::expected<int, my_error_t> fallible_function2_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function3_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function4_group18();

tl::expected<int, my_error_t> fallible_function3_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function4_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function5_group18();

tl::expected<int, my_error_t> fallible_function4_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function5_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function6_group18();

tl::expected<int, my_error_t> fallible_function5_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function6_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function7_group18();

tl::expected<int, my_error_t> fallible_function6_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function7_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function8_group18();

tl::expected<int, my_error_t> fallible_function7_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function8_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function9_group18();

tl::expected<int, my_error_t> fallible_function8_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function9_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function10_group18();

tl::expected<int, my_error_t> fallible_function9_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function10_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function11_group18();

tl::expected<int, my_error_t> fallible_function10_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function11_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function12_group18();

tl::expected<int, my_error_t> fallible_function11_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function12_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function13_group18();

tl::expected<int, my_error_t> fallible_function12_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function13_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function14_group18();

tl::expected<int, my_error_t> fallible_function13_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function14_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function15_group18();

tl::expected<int, my_error_t> fallible_function14_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function15_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function16_group18();

tl::expected<int, my_error_t> fallible_function15_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function16_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function17_group18();

tl::expected<int, my_error_t> fallible_function16_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function17_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function18_group18();

tl::expected<int, my_error_t> fallible_function17_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function18_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function19_group18();

tl::expected<int, my_error_t> fallible_function18_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function19_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function20_group18();

tl::expected<int, my_error_t> fallible_function19_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function20_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function21_group18();

tl::expected<int, my_error_t> fallible_function20_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function21_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function22_group18();

tl::expected<int, my_error_t> fallible_function21_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function22_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function23_group18();

tl::expected<int, my_error_t> fallible_function22_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function23_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function24_group18();

tl::expected<int, my_error_t> fallible_function23_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function24_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function25_group18();

tl::expected<int, my_error_t> fallible_function24_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function25_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function26_group18();

tl::expected<int, my_error_t> fallible_function25_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function26_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function27_group18();

tl::expected<int, my_error_t> fallible_function26_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function27_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function28_group18();

tl::expected<int, my_error_t> fallible_function27_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function28_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function29_group18();

tl::expected<int, my_error_t> fallible_function28_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function29_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function30_group18();

tl::expected<int, my_error_t> fallible_function29_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function30_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function31_group18();

tl::expected<int, my_error_t> fallible_function30_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function31_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function32_group18();

tl::expected<int, my_error_t> fallible_function31_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function32_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function33_group18();

tl::expected<int, my_error_t> fallible_function32_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function33_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function34_group18();

tl::expected<int, my_error_t> fallible_function33_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function34_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function35_group18();

tl::expected<int, my_error_t> fallible_function34_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function35_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function36_group18();

tl::expected<int, my_error_t> fallible_function35_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function36_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function37_group18();

tl::expected<int, my_error_t> fallible_function36_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function37_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function38_group18();

tl::expected<int, my_error_t> fallible_function37_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function38_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function39_group18();

tl::expected<int, my_error_t> fallible_function38_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function39_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function40_group18();

tl::expected<int, my_error_t> fallible_function39_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function40_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function41_group18();

tl::expected<int, my_error_t> fallible_function40_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function41_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function42_group18();

tl::expected<int, my_error_t> fallible_function41_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function42_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function43_group18();

tl::expected<int, my_error_t> fallible_function42_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function43_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function44_group18();

tl::expected<int, my_error_t> fallible_function43_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function44_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function45_group18();

tl::expected<int, my_error_t> fallible_function44_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function45_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function46_group18();

tl::expected<int, my_error_t> fallible_function45_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function46_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function47_group18();

tl::expected<int, my_error_t> fallible_function46_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function47_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function48_group18();

tl::expected<int, my_error_t> fallible_function47_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function48_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function49_group18();

tl::expected<int, my_error_t> fallible_function48_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function49_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function50_group18();

tl::expected<int, my_error_t> fallible_function49_group18()
{
  auto result_a = non_trivial_destructor::initialize(18);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(18);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function50_group18(); result) {
    side_effect18 = side_effect18 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18;
}

tl::expected<int, my_error_t> fallible_function50_group18()
{
  if (auto result = non_trivial_destructor::initialize(50); result) {
    auto result_internal = result.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect18 + 1;
}

tl::expected<int, my_error_t> fallible_function1_group19();

tl::expected<int, my_error_t> fallible_function0_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function1_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function2_group19();

tl::expected<int, my_error_t> fallible_function1_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function2_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function3_group19();

tl::expected<int, my_error_t> fallible_function2_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function3_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function4_group19();

tl::expected<int, my_error_t> fallible_function3_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function4_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function5_group19();

tl::expected<int, my_error_t> fallible_function4_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function5_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function6_group19();

tl::expected<int, my_error_t> fallible_function5_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function6_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function7_group19();

tl::expected<int, my_error_t> fallible_function6_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function7_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function8_group19();

tl::expected<int, my_error_t> fallible_function7_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function8_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function9_group19();

tl::expected<int, my_error_t> fallible_function8_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function9_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function10_group19();

tl::expected<int, my_error_t> fallible_function9_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function10_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function11_group19();

tl::expected<int, my_error_t> fallible_function10_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function11_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function12_group19();

tl::expected<int, my_error_t> fallible_function11_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function12_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function13_group19();

tl::expected<int, my_error_t> fallible_function12_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function13_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function14_group19();

tl::expected<int, my_error_t> fallible_function13_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function14_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function15_group19();

tl::expected<int, my_error_t> fallible_function14_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function15_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function16_group19();

tl::expected<int, my_error_t> fallible_function15_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function16_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function17_group19();

tl::expected<int, my_error_t> fallible_function16_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function17_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function18_group19();

tl::expected<int, my_error_t> fallible_function17_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function18_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function19_group19();

tl::expected<int, my_error_t> fallible_function18_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function19_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function20_group19();

tl::expected<int, my_error_t> fallible_function19_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function20_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function21_group19();

tl::expected<int, my_error_t> fallible_function20_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function21_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function22_group19();

tl::expected<int, my_error_t> fallible_function21_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function22_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function23_group19();

tl::expected<int, my_error_t> fallible_function22_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function23_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function24_group19();

tl::expected<int, my_error_t> fallible_function23_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function24_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function25_group19();

tl::expected<int, my_error_t> fallible_function24_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function25_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function26_group19();

tl::expected<int, my_error_t> fallible_function25_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function26_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function27_group19();

tl::expected<int, my_error_t> fallible_function26_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function27_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function28_group19();

tl::expected<int, my_error_t> fallible_function27_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function28_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function29_group19();

tl::expected<int, my_error_t> fallible_function28_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function29_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function30_group19();

tl::expected<int, my_error_t> fallible_function29_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function30_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function31_group19();

tl::expected<int, my_error_t> fallible_function30_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function31_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function32_group19();

tl::expected<int, my_error_t> fallible_function31_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function32_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function33_group19();

tl::expected<int, my_error_t> fallible_function32_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function33_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function34_group19();

tl::expected<int, my_error_t> fallible_function33_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function34_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function35_group19();

tl::expected<int, my_error_t> fallible_function34_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function35_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function36_group19();

tl::expected<int, my_error_t> fallible_function35_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function36_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function37_group19();

tl::expected<int, my_error_t> fallible_function36_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function37_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function38_group19();

tl::expected<int, my_error_t> fallible_function37_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function38_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function39_group19();

tl::expected<int, my_error_t> fallible_function38_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function39_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function40_group19();

tl::expected<int, my_error_t> fallible_function39_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function40_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function41_group19();

tl::expected<int, my_error_t> fallible_function40_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function41_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function42_group19();

tl::expected<int, my_error_t> fallible_function41_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function42_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function43_group19();

tl::expected<int, my_error_t> fallible_function42_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function43_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function44_group19();

tl::expected<int, my_error_t> fallible_function43_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function44_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function45_group19();

tl::expected<int, my_error_t> fallible_function44_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function45_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function46_group19();

tl::expected<int, my_error_t> fallible_function45_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function46_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function47_group19();

tl::expected<int, my_error_t> fallible_function46_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function47_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function48_group19();

tl::expected<int, my_error_t> fallible_function47_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function48_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function49_group19();

tl::expected<int, my_error_t> fallible_function48_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function49_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function50_group19();

tl::expected<int, my_error_t> fallible_function49_group19()
{
  auto result_a = non_trivial_destructor::initialize(19);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(19);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function50_group19(); result) {
    side_effect19 = side_effect19 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19;
}

tl::expected<int, my_error_t> fallible_function50_group19()
{
  if (auto result = non_trivial_destructor::initialize(50); result) {
    auto result_internal = result.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect19 + 1;
}

tl::expected<int, my_error_t> fallible_function1_group20();

tl::expected<int, my_error_t> fallible_function0_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function1_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function2_group20();

tl::expected<int, my_error_t> fallible_function1_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function2_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function3_group20();

tl::expected<int, my_error_t> fallible_function2_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function3_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function4_group20();

tl::expected<int, my_error_t> fallible_function3_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function4_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function5_group20();

tl::expected<int, my_error_t> fallible_function4_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function5_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function6_group20();

tl::expected<int, my_error_t> fallible_function5_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function6_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function7_group20();

tl::expected<int, my_error_t> fallible_function6_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function7_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function8_group20();

tl::expected<int, my_error_t> fallible_function7_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function8_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function9_group20();

tl::expected<int, my_error_t> fallible_function8_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function9_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function10_group20();

tl::expected<int, my_error_t> fallible_function9_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function10_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function11_group20();

tl::expected<int, my_error_t> fallible_function10_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function11_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function12_group20();

tl::expected<int, my_error_t> fallible_function11_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function12_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function13_group20();

tl::expected<int, my_error_t> fallible_function12_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function13_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function14_group20();

tl::expected<int, my_error_t> fallible_function13_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function14_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function15_group20();

tl::expected<int, my_error_t> fallible_function14_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function15_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function16_group20();

tl::expected<int, my_error_t> fallible_function15_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function16_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function17_group20();

tl::expected<int, my_error_t> fallible_function16_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function17_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function18_group20();

tl::expected<int, my_error_t> fallible_function17_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function18_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function19_group20();

tl::expected<int, my_error_t> fallible_function18_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function19_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function20_group20();

tl::expected<int, my_error_t> fallible_function19_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function20_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function21_group20();

tl::expected<int, my_error_t> fallible_function20_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function21_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function22_group20();

tl::expected<int, my_error_t> fallible_function21_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function22_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function23_group20();

tl::expected<int, my_error_t> fallible_function22_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function23_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function24_group20();

tl::expected<int, my_error_t> fallible_function23_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function24_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function25_group20();

tl::expected<int, my_error_t> fallible_function24_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function25_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function26_group20();

tl::expected<int, my_error_t> fallible_function25_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function26_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function27_group20();

tl::expected<int, my_error_t> fallible_function26_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function27_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function28_group20();

tl::expected<int, my_error_t> fallible_function27_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function28_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function29_group20();

tl::expected<int, my_error_t> fallible_function28_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function29_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function30_group20();

tl::expected<int, my_error_t> fallible_function29_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function30_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function31_group20();

tl::expected<int, my_error_t> fallible_function30_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function31_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function32_group20();

tl::expected<int, my_error_t> fallible_function31_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function32_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function33_group20();

tl::expected<int, my_error_t> fallible_function32_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function33_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function34_group20();

tl::expected<int, my_error_t> fallible_function33_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function34_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function35_group20();

tl::expected<int, my_error_t> fallible_function34_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function35_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function36_group20();

tl::expected<int, my_error_t> fallible_function35_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function36_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function37_group20();

tl::expected<int, my_error_t> fallible_function36_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function37_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function38_group20();

tl::expected<int, my_error_t> fallible_function37_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function38_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function39_group20();

tl::expected<int, my_error_t> fallible_function38_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function39_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function40_group20();

tl::expected<int, my_error_t> fallible_function39_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function40_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function41_group20();

tl::expected<int, my_error_t> fallible_function40_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function41_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function42_group20();

tl::expected<int, my_error_t> fallible_function41_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function42_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function43_group20();

tl::expected<int, my_error_t> fallible_function42_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function43_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function44_group20();

tl::expected<int, my_error_t> fallible_function43_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function44_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function45_group20();

tl::expected<int, my_error_t> fallible_function44_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function45_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function46_group20();

tl::expected<int, my_error_t> fallible_function45_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function46_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function47_group20();

tl::expected<int, my_error_t> fallible_function46_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function47_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function48_group20();

tl::expected<int, my_error_t> fallible_function47_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function48_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function49_group20();

tl::expected<int, my_error_t> fallible_function48_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function49_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function50_group20();

tl::expected<int, my_error_t> fallible_function49_group20()
{
  auto result_a = non_trivial_destructor::initialize(20);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(20);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function50_group20(); result) {
    side_effect20 = side_effect20 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20;
}

tl::expected<int, my_error_t> fallible_function50_group20()
{
  if (auto result = non_trivial_destructor::initialize(50); result) {
    auto result_internal = result.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect20 + 1;
}

tl::expected<int, my_error_t> fallible_function1_group21();

tl::expected<int, my_error_t> fallible_function0_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function1_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function2_group21();

tl::expected<int, my_error_t> fallible_function1_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function2_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function3_group21();

tl::expected<int, my_error_t> fallible_function2_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function3_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function4_group21();

tl::expected<int, my_error_t> fallible_function3_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function4_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function5_group21();

tl::expected<int, my_error_t> fallible_function4_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function5_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function6_group21();

tl::expected<int, my_error_t> fallible_function5_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function6_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function7_group21();

tl::expected<int, my_error_t> fallible_function6_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function7_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function8_group21();

tl::expected<int, my_error_t> fallible_function7_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function8_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function9_group21();

tl::expected<int, my_error_t> fallible_function8_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function9_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function10_group21();

tl::expected<int, my_error_t> fallible_function9_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function10_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function11_group21();

tl::expected<int, my_error_t> fallible_function10_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function11_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function12_group21();

tl::expected<int, my_error_t> fallible_function11_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function12_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function13_group21();

tl::expected<int, my_error_t> fallible_function12_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function13_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function14_group21();

tl::expected<int, my_error_t> fallible_function13_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function14_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function15_group21();

tl::expected<int, my_error_t> fallible_function14_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function15_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function16_group21();

tl::expected<int, my_error_t> fallible_function15_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function16_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function17_group21();

tl::expected<int, my_error_t> fallible_function16_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function17_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function18_group21();

tl::expected<int, my_error_t> fallible_function17_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function18_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function19_group21();

tl::expected<int, my_error_t> fallible_function18_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function19_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function20_group21();

tl::expected<int, my_error_t> fallible_function19_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function20_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function21_group21();

tl::expected<int, my_error_t> fallible_function20_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function21_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function22_group21();

tl::expected<int, my_error_t> fallible_function21_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function22_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function23_group21();

tl::expected<int, my_error_t> fallible_function22_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function23_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function24_group21();

tl::expected<int, my_error_t> fallible_function23_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function24_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function25_group21();

tl::expected<int, my_error_t> fallible_function24_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function25_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function26_group21();

tl::expected<int, my_error_t> fallible_function25_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function26_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function27_group21();

tl::expected<int, my_error_t> fallible_function26_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function27_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function28_group21();

tl::expected<int, my_error_t> fallible_function27_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function28_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function29_group21();

tl::expected<int, my_error_t> fallible_function28_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function29_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function30_group21();

tl::expected<int, my_error_t> fallible_function29_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function30_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function31_group21();

tl::expected<int, my_error_t> fallible_function30_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function31_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function32_group21();

tl::expected<int, my_error_t> fallible_function31_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function32_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function33_group21();

tl::expected<int, my_error_t> fallible_function32_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function33_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function34_group21();

tl::expected<int, my_error_t> fallible_function33_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function34_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function35_group21();

tl::expected<int, my_error_t> fallible_function34_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function35_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function36_group21();

tl::expected<int, my_error_t> fallible_function35_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function36_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function37_group21();

tl::expected<int, my_error_t> fallible_function36_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function37_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function38_group21();

tl::expected<int, my_error_t> fallible_function37_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function38_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function39_group21();

tl::expected<int, my_error_t> fallible_function38_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function39_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function40_group21();

tl::expected<int, my_error_t> fallible_function39_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function40_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function41_group21();

tl::expected<int, my_error_t> fallible_function40_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function41_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function42_group21();

tl::expected<int, my_error_t> fallible_function41_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function42_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function43_group21();

tl::expected<int, my_error_t> fallible_function42_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function43_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function44_group21();

tl::expected<int, my_error_t> fallible_function43_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function44_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function45_group21();

tl::expected<int, my_error_t> fallible_function44_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function45_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function46_group21();

tl::expected<int, my_error_t> fallible_function45_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function46_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function47_group21();

tl::expected<int, my_error_t> fallible_function46_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function47_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function48_group21();

tl::expected<int, my_error_t> fallible_function47_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function48_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function49_group21();

tl::expected<int, my_error_t> fallible_function48_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function49_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function50_group21();

tl::expected<int, my_error_t> fallible_function49_group21()
{
  auto result_a = non_trivial_destructor::initialize(21);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(21);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function50_group21(); result) {
    side_effect21 = side_effect21 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21;
}

tl::expected<int, my_error_t> fallible_function50_group21()
{
  if (auto result = non_trivial_destructor::initialize(50); result) {
    auto result_internal = result.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect21 + 1;
}

tl::expected<int, my_error_t> fallible_function1_group22();

tl::expected<int, my_error_t> fallible_function0_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function1_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function2_group22();

tl::expected<int, my_error_t> fallible_function1_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function2_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function3_group22();

tl::expected<int, my_error_t> fallible_function2_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function3_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function4_group22();

tl::expected<int, my_error_t> fallible_function3_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function4_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function5_group22();

tl::expected<int, my_error_t> fallible_function4_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function5_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function6_group22();

tl::expected<int, my_error_t> fallible_function5_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function6_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function7_group22();

tl::expected<int, my_error_t> fallible_function6_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function7_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function8_group22();

tl::expected<int, my_error_t> fallible_function7_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function8_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function9_group22();

tl::expected<int, my_error_t> fallible_function8_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function9_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function10_group22();

tl::expected<int, my_error_t> fallible_function9_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function10_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function11_group22();

tl::expected<int, my_error_t> fallible_function10_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function11_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function12_group22();

tl::expected<int, my_error_t> fallible_function11_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function12_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function13_group22();

tl::expected<int, my_error_t> fallible_function12_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function13_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function14_group22();

tl::expected<int, my_error_t> fallible_function13_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function14_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function15_group22();

tl::expected<int, my_error_t> fallible_function14_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function15_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function16_group22();

tl::expected<int, my_error_t> fallible_function15_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function16_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function17_group22();

tl::expected<int, my_error_t> fallible_function16_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function17_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function18_group22();

tl::expected<int, my_error_t> fallible_function17_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function18_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function19_group22();

tl::expected<int, my_error_t> fallible_function18_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function19_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function20_group22();

tl::expected<int, my_error_t> fallible_function19_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function20_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function21_group22();

tl::expected<int, my_error_t> fallible_function20_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function21_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function22_group22();

tl::expected<int, my_error_t> fallible_function21_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function22_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function23_group22();

tl::expected<int, my_error_t> fallible_function22_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function23_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function24_group22();

tl::expected<int, my_error_t> fallible_function23_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function24_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function25_group22();

tl::expected<int, my_error_t> fallible_function24_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function25_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function26_group22();

tl::expected<int, my_error_t> fallible_function25_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function26_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function27_group22();

tl::expected<int, my_error_t> fallible_function26_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function27_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function28_group22();

tl::expected<int, my_error_t> fallible_function27_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function28_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function29_group22();

tl::expected<int, my_error_t> fallible_function28_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function29_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function30_group22();

tl::expected<int, my_error_t> fallible_function29_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function30_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function31_group22();

tl::expected<int, my_error_t> fallible_function30_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function31_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function32_group22();

tl::expected<int, my_error_t> fallible_function31_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function32_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function33_group22();

tl::expected<int, my_error_t> fallible_function32_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function33_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function34_group22();

tl::expected<int, my_error_t> fallible_function33_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function34_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function35_group22();

tl::expected<int, my_error_t> fallible_function34_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function35_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function36_group22();

tl::expected<int, my_error_t> fallible_function35_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function36_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function37_group22();

tl::expected<int, my_error_t> fallible_function36_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function37_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function38_group22();

tl::expected<int, my_error_t> fallible_function37_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function38_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function39_group22();

tl::expected<int, my_error_t> fallible_function38_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function39_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function40_group22();

tl::expected<int, my_error_t> fallible_function39_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function40_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function41_group22();

tl::expected<int, my_error_t> fallible_function40_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function41_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function42_group22();

tl::expected<int, my_error_t> fallible_function41_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function42_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function43_group22();

tl::expected<int, my_error_t> fallible_function42_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function43_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function44_group22();

tl::expected<int, my_error_t> fallible_function43_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function44_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function45_group22();

tl::expected<int, my_error_t> fallible_function44_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function45_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function46_group22();

tl::expected<int, my_error_t> fallible_function45_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function46_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function47_group22();

tl::expected<int, my_error_t> fallible_function46_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function47_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function48_group22();

tl::expected<int, my_error_t> fallible_function47_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function48_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function49_group22();

tl::expected<int, my_error_t> fallible_function48_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function49_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function50_group22();

tl::expected<int, my_error_t> fallible_function49_group22()
{
  auto result_a = non_trivial_destructor::initialize(22);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(22);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function50_group22(); result) {
    side_effect22 = side_effect22 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22;
}

tl::expected<int, my_error_t> fallible_function50_group22()
{
  if (auto result = non_trivial_destructor::initialize(50); result) {
    auto result_internal = result.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect22 + 1;
}

tl::expected<int, my_error_t> fallible_function1_group23();

tl::expected<int, my_error_t> fallible_function0_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function1_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function2_group23();

tl::expected<int, my_error_t> fallible_function1_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function2_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function3_group23();

tl::expected<int, my_error_t> fallible_function2_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function3_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function4_group23();

tl::expected<int, my_error_t> fallible_function3_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function4_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function5_group23();

tl::expected<int, my_error_t> fallible_function4_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function5_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function6_group23();

tl::expected<int, my_error_t> fallible_function5_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function6_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function7_group23();

tl::expected<int, my_error_t> fallible_function6_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function7_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function8_group23();

tl::expected<int, my_error_t> fallible_function7_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function8_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function9_group23();

tl::expected<int, my_error_t> fallible_function8_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function9_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function10_group23();

tl::expected<int, my_error_t> fallible_function9_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function10_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function11_group23();

tl::expected<int, my_error_t> fallible_function10_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function11_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function12_group23();

tl::expected<int, my_error_t> fallible_function11_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function12_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function13_group23();

tl::expected<int, my_error_t> fallible_function12_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function13_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function14_group23();

tl::expected<int, my_error_t> fallible_function13_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function14_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function15_group23();

tl::expected<int, my_error_t> fallible_function14_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function15_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function16_group23();

tl::expected<int, my_error_t> fallible_function15_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function16_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function17_group23();

tl::expected<int, my_error_t> fallible_function16_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function17_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function18_group23();

tl::expected<int, my_error_t> fallible_function17_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function18_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function19_group23();

tl::expected<int, my_error_t> fallible_function18_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function19_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function20_group23();

tl::expected<int, my_error_t> fallible_function19_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function20_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function21_group23();

tl::expected<int, my_error_t> fallible_function20_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function21_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function22_group23();

tl::expected<int, my_error_t> fallible_function21_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function22_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function23_group23();

tl::expected<int, my_error_t> fallible_function22_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function23_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function24_group23();

tl::expected<int, my_error_t> fallible_function23_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function24_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function25_group23();

tl::expected<int, my_error_t> fallible_function24_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function25_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function26_group23();

tl::expected<int, my_error_t> fallible_function25_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function26_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function27_group23();

tl::expected<int, my_error_t> fallible_function26_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function27_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function28_group23();

tl::expected<int, my_error_t> fallible_function27_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function28_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function29_group23();

tl::expected<int, my_error_t> fallible_function28_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function29_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function30_group23();

tl::expected<int, my_error_t> fallible_function29_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function30_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function31_group23();

tl::expected<int, my_error_t> fallible_function30_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function31_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function32_group23();

tl::expected<int, my_error_t> fallible_function31_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function32_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function33_group23();

tl::expected<int, my_error_t> fallible_function32_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function33_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function34_group23();

tl::expected<int, my_error_t> fallible_function33_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function34_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function35_group23();

tl::expected<int, my_error_t> fallible_function34_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function35_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function36_group23();

tl::expected<int, my_error_t> fallible_function35_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function36_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function37_group23();

tl::expected<int, my_error_t> fallible_function36_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function37_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function38_group23();

tl::expected<int, my_error_t> fallible_function37_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function38_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function39_group23();

tl::expected<int, my_error_t> fallible_function38_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function39_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function40_group23();

tl::expected<int, my_error_t> fallible_function39_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function40_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function41_group23();

tl::expected<int, my_error_t> fallible_function40_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function41_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function42_group23();

tl::expected<int, my_error_t> fallible_function41_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function42_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function43_group23();

tl::expected<int, my_error_t> fallible_function42_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function43_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function44_group23();

tl::expected<int, my_error_t> fallible_function43_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function44_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function45_group23();

tl::expected<int, my_error_t> fallible_function44_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function45_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function46_group23();

tl::expected<int, my_error_t> fallible_function45_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function46_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function47_group23();

tl::expected<int, my_error_t> fallible_function46_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function47_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function48_group23();

tl::expected<int, my_error_t> fallible_function47_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function48_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function49_group23();

tl::expected<int, my_error_t> fallible_function48_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function49_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function50_group23();

tl::expected<int, my_error_t> fallible_function49_group23()
{
  auto result_a = non_trivial_destructor::initialize(23);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(23);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function50_group23(); result) {
    side_effect23 = side_effect23 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23;
}

tl::expected<int, my_error_t> fallible_function50_group23()
{
  if (auto result = non_trivial_destructor::initialize(50); result) {
    auto result_internal = result.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect23 + 1;
}

tl::expected<int, my_error_t> fallible_function1_group24();

tl::expected<int, my_error_t> fallible_function0_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function1_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function2_group24();

tl::expected<int, my_error_t> fallible_function1_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function2_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function3_group24();

tl::expected<int, my_error_t> fallible_function2_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function3_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function4_group24();

tl::expected<int, my_error_t> fallible_function3_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function4_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function5_group24();

tl::expected<int, my_error_t> fallible_function4_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function5_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function6_group24();

tl::expected<int, my_error_t> fallible_function5_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function6_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function7_group24();

tl::expected<int, my_error_t> fallible_function6_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function7_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function8_group24();

tl::expected<int, my_error_t> fallible_function7_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function8_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function9_group24();

tl::expected<int, my_error_t> fallible_function8_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function9_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function10_group24();

tl::expected<int, my_error_t> fallible_function9_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function10_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function11_group24();

tl::expected<int, my_error_t> fallible_function10_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function11_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function12_group24();

tl::expected<int, my_error_t> fallible_function11_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function12_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function13_group24();

tl::expected<int, my_error_t> fallible_function12_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function13_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function14_group24();

tl::expected<int, my_error_t> fallible_function13_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function14_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function15_group24();

tl::expected<int, my_error_t> fallible_function14_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function15_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function16_group24();

tl::expected<int, my_error_t> fallible_function15_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function16_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function17_group24();

tl::expected<int, my_error_t> fallible_function16_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function17_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function18_group24();

tl::expected<int, my_error_t> fallible_function17_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function18_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function19_group24();

tl::expected<int, my_error_t> fallible_function18_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function19_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function20_group24();

tl::expected<int, my_error_t> fallible_function19_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function20_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function21_group24();

tl::expected<int, my_error_t> fallible_function20_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function21_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function22_group24();

tl::expected<int, my_error_t> fallible_function21_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function22_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function23_group24();

tl::expected<int, my_error_t> fallible_function22_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function23_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function24_group24();

tl::expected<int, my_error_t> fallible_function23_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function24_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function25_group24();

tl::expected<int, my_error_t> fallible_function24_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function25_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function26_group24();

tl::expected<int, my_error_t> fallible_function25_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function26_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function27_group24();

tl::expected<int, my_error_t> fallible_function26_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function27_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function28_group24();

tl::expected<int, my_error_t> fallible_function27_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function28_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function29_group24();

tl::expected<int, my_error_t> fallible_function28_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function29_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function30_group24();

tl::expected<int, my_error_t> fallible_function29_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function30_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function31_group24();

tl::expected<int, my_error_t> fallible_function30_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function31_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function32_group24();

tl::expected<int, my_error_t> fallible_function31_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function32_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function33_group24();

tl::expected<int, my_error_t> fallible_function32_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function33_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function34_group24();

tl::expected<int, my_error_t> fallible_function33_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function34_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function35_group24();

tl::expected<int, my_error_t> fallible_function34_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function35_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function36_group24();

tl::expected<int, my_error_t> fallible_function35_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function36_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function37_group24();

tl::expected<int, my_error_t> fallible_function36_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function37_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function38_group24();

tl::expected<int, my_error_t> fallible_function37_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function38_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function39_group24();

tl::expected<int, my_error_t> fallible_function38_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function39_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function40_group24();

tl::expected<int, my_error_t> fallible_function39_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function40_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function41_group24();

tl::expected<int, my_error_t> fallible_function40_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function41_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function42_group24();

tl::expected<int, my_error_t> fallible_function41_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function42_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function43_group24();

tl::expected<int, my_error_t> fallible_function42_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function43_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function44_group24();

tl::expected<int, my_error_t> fallible_function43_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function44_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function45_group24();

tl::expected<int, my_error_t> fallible_function44_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function45_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function46_group24();

tl::expected<int, my_error_t> fallible_function45_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function46_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function47_group24();

tl::expected<int, my_error_t> fallible_function46_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function47_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function48_group24();

tl::expected<int, my_error_t> fallible_function47_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function48_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function49_group24();

tl::expected<int, my_error_t> fallible_function48_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function49_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function50_group24();

tl::expected<int, my_error_t> fallible_function49_group24()
{
  auto result_a = non_trivial_destructor::initialize(24);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(24);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function50_group24(); result) {
    side_effect24 = side_effect24 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24;
}

tl::expected<int, my_error_t> fallible_function50_group24()
{
  if (auto result = non_trivial_destructor::initialize(50); result) {
    auto result_internal = result.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect24 + 1;
}

tl::expected<int, my_error_t> fallible_function1_group25();

tl::expected<int, my_error_t> fallible_function0_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function1_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function2_group25();

tl::expected<int, my_error_t> fallible_function1_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function2_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function3_group25();

tl::expected<int, my_error_t> fallible_function2_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function3_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function4_group25();

tl::expected<int, my_error_t> fallible_function3_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function4_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function5_group25();

tl::expected<int, my_error_t> fallible_function4_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function5_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function6_group25();

tl::expected<int, my_error_t> fallible_function5_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function6_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function7_group25();

tl::expected<int, my_error_t> fallible_function6_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function7_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function8_group25();

tl::expected<int, my_error_t> fallible_function7_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function8_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function9_group25();

tl::expected<int, my_error_t> fallible_function8_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function9_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function10_group25();

tl::expected<int, my_error_t> fallible_function9_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function10_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function11_group25();

tl::expected<int, my_error_t> fallible_function10_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function11_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function12_group25();

tl::expected<int, my_error_t> fallible_function11_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function12_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function13_group25();

tl::expected<int, my_error_t> fallible_function12_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function13_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function14_group25();

tl::expected<int, my_error_t> fallible_function13_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function14_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function15_group25();

tl::expected<int, my_error_t> fallible_function14_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function15_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function16_group25();

tl::expected<int, my_error_t> fallible_function15_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function16_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function17_group25();

tl::expected<int, my_error_t> fallible_function16_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function17_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function18_group25();

tl::expected<int, my_error_t> fallible_function17_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function18_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function19_group25();

tl::expected<int, my_error_t> fallible_function18_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function19_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function20_group25();

tl::expected<int, my_error_t> fallible_function19_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function20_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function21_group25();

tl::expected<int, my_error_t> fallible_function20_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function21_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function22_group25();

tl::expected<int, my_error_t> fallible_function21_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function22_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function23_group25();

tl::expected<int, my_error_t> fallible_function22_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function23_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function24_group25();

tl::expected<int, my_error_t> fallible_function23_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function24_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function25_group25();

tl::expected<int, my_error_t> fallible_function24_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function25_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function26_group25();

tl::expected<int, my_error_t> fallible_function25_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function26_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function27_group25();

tl::expected<int, my_error_t> fallible_function26_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function27_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function28_group25();

tl::expected<int, my_error_t> fallible_function27_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function28_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function29_group25();

tl::expected<int, my_error_t> fallible_function28_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function29_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function30_group25();

tl::expected<int, my_error_t> fallible_function29_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function30_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function31_group25();

tl::expected<int, my_error_t> fallible_function30_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function31_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function32_group25();

tl::expected<int, my_error_t> fallible_function31_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function32_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function33_group25();

tl::expected<int, my_error_t> fallible_function32_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function33_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function34_group25();

tl::expected<int, my_error_t> fallible_function33_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function34_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function35_group25();

tl::expected<int, my_error_t> fallible_function34_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function35_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function36_group25();

tl::expected<int, my_error_t> fallible_function35_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function36_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function37_group25();

tl::expected<int, my_error_t> fallible_function36_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function37_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function38_group25();

tl::expected<int, my_error_t> fallible_function37_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function38_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function39_group25();

tl::expected<int, my_error_t> fallible_function38_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function39_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function40_group25();

tl::expected<int, my_error_t> fallible_function39_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function40_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function41_group25();

tl::expected<int, my_error_t> fallible_function40_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function41_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function42_group25();

tl::expected<int, my_error_t> fallible_function41_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function42_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function43_group25();

tl::expected<int, my_error_t> fallible_function42_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function43_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function44_group25();

tl::expected<int, my_error_t> fallible_function43_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function44_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function45_group25();

tl::expected<int, my_error_t> fallible_function44_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function45_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function46_group25();

tl::expected<int, my_error_t> fallible_function45_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function46_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function47_group25();

tl::expected<int, my_error_t> fallible_function46_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function47_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function48_group25();

tl::expected<int, my_error_t> fallible_function47_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function48_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function49_group25();

tl::expected<int, my_error_t> fallible_function48_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function49_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function50_group25();

tl::expected<int, my_error_t> fallible_function49_group25()
{
  auto result_a = non_trivial_destructor::initialize(25);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(25);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function50_group25(); result) {
    side_effect25 = side_effect25 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25;
}

tl::expected<int, my_error_t> fallible_function50_group25()
{
  if (auto result = non_trivial_destructor::initialize(50); result) {
    auto result_internal = result.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect25 + 1;
}

tl::expected<int, my_error_t> fallible_function1_group26();

tl::expected<int, my_error_t> fallible_function0_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function1_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function2_group26();

tl::expected<int, my_error_t> fallible_function1_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function2_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function3_group26();

tl::expected<int, my_error_t> fallible_function2_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function3_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function4_group26();

tl::expected<int, my_error_t> fallible_function3_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function4_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function5_group26();

tl::expected<int, my_error_t> fallible_function4_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function5_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function6_group26();

tl::expected<int, my_error_t> fallible_function5_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function6_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function7_group26();

tl::expected<int, my_error_t> fallible_function6_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function7_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function8_group26();

tl::expected<int, my_error_t> fallible_function7_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function8_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function9_group26();

tl::expected<int, my_error_t> fallible_function8_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function9_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function10_group26();

tl::expected<int, my_error_t> fallible_function9_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function10_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function11_group26();

tl::expected<int, my_error_t> fallible_function10_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function11_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function12_group26();

tl::expected<int, my_error_t> fallible_function11_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function12_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function13_group26();

tl::expected<int, my_error_t> fallible_function12_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function13_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function14_group26();

tl::expected<int, my_error_t> fallible_function13_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function14_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function15_group26();

tl::expected<int, my_error_t> fallible_function14_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function15_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function16_group26();

tl::expected<int, my_error_t> fallible_function15_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function16_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function17_group26();

tl::expected<int, my_error_t> fallible_function16_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function17_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function18_group26();

tl::expected<int, my_error_t> fallible_function17_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function18_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function19_group26();

tl::expected<int, my_error_t> fallible_function18_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function19_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function20_group26();

tl::expected<int, my_error_t> fallible_function19_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function20_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function21_group26();

tl::expected<int, my_error_t> fallible_function20_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function21_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function22_group26();

tl::expected<int, my_error_t> fallible_function21_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function22_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function23_group26();

tl::expected<int, my_error_t> fallible_function22_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function23_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function24_group26();

tl::expected<int, my_error_t> fallible_function23_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function24_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function25_group26();

tl::expected<int, my_error_t> fallible_function24_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function25_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function26_group26();

tl::expected<int, my_error_t> fallible_function25_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function26_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function27_group26();

tl::expected<int, my_error_t> fallible_function26_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function27_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function28_group26();

tl::expected<int, my_error_t> fallible_function27_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function28_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function29_group26();

tl::expected<int, my_error_t> fallible_function28_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function29_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function30_group26();

tl::expected<int, my_error_t> fallible_function29_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function30_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function31_group26();

tl::expected<int, my_error_t> fallible_function30_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function31_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function32_group26();

tl::expected<int, my_error_t> fallible_function31_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function32_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function33_group26();

tl::expected<int, my_error_t> fallible_function32_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function33_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function34_group26();

tl::expected<int, my_error_t> fallible_function33_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function34_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function35_group26();

tl::expected<int, my_error_t> fallible_function34_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function35_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function36_group26();

tl::expected<int, my_error_t> fallible_function35_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function36_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function37_group26();

tl::expected<int, my_error_t> fallible_function36_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function37_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function38_group26();

tl::expected<int, my_error_t> fallible_function37_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function38_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function39_group26();

tl::expected<int, my_error_t> fallible_function38_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function39_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function40_group26();

tl::expected<int, my_error_t> fallible_function39_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function40_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function41_group26();

tl::expected<int, my_error_t> fallible_function40_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function41_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function42_group26();

tl::expected<int, my_error_t> fallible_function41_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function42_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function43_group26();

tl::expected<int, my_error_t> fallible_function42_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function43_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function44_group26();

tl::expected<int, my_error_t> fallible_function43_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function44_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function45_group26();

tl::expected<int, my_error_t> fallible_function44_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function45_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function46_group26();

tl::expected<int, my_error_t> fallible_function45_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function46_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function47_group26();

tl::expected<int, my_error_t> fallible_function46_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function47_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function48_group26();

tl::expected<int, my_error_t> fallible_function47_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function48_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function49_group26();

tl::expected<int, my_error_t> fallible_function48_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function49_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function50_group26();

tl::expected<int, my_error_t> fallible_function49_group26()
{
  auto result_a = non_trivial_destructor::initialize(26);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(26);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function50_group26(); result) {
    side_effect26 = side_effect26 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26;
}

tl::expected<int, my_error_t> fallible_function50_group26()
{
  if (auto result = non_trivial_destructor::initialize(50); result) {
    auto result_internal = result.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect26 + 1;
}

tl::expected<int, my_error_t> fallible_function1_group27();

tl::expected<int, my_error_t> fallible_function0_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function1_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function2_group27();

tl::expected<int, my_error_t> fallible_function1_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function2_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function3_group27();

tl::expected<int, my_error_t> fallible_function2_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function3_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function4_group27();

tl::expected<int, my_error_t> fallible_function3_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function4_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function5_group27();

tl::expected<int, my_error_t> fallible_function4_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function5_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function6_group27();

tl::expected<int, my_error_t> fallible_function5_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function6_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function7_group27();

tl::expected<int, my_error_t> fallible_function6_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function7_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function8_group27();

tl::expected<int, my_error_t> fallible_function7_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function8_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function9_group27();

tl::expected<int, my_error_t> fallible_function8_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function9_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function10_group27();

tl::expected<int, my_error_t> fallible_function9_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function10_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function11_group27();

tl::expected<int, my_error_t> fallible_function10_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function11_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function12_group27();

tl::expected<int, my_error_t> fallible_function11_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function12_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function13_group27();

tl::expected<int, my_error_t> fallible_function12_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function13_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function14_group27();

tl::expected<int, my_error_t> fallible_function13_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function14_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function15_group27();

tl::expected<int, my_error_t> fallible_function14_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function15_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function16_group27();

tl::expected<int, my_error_t> fallible_function15_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function16_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function17_group27();

tl::expected<int, my_error_t> fallible_function16_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function17_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function18_group27();

tl::expected<int, my_error_t> fallible_function17_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function18_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function19_group27();

tl::expected<int, my_error_t> fallible_function18_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function19_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function20_group27();

tl::expected<int, my_error_t> fallible_function19_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function20_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function21_group27();

tl::expected<int, my_error_t> fallible_function20_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function21_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function22_group27();

tl::expected<int, my_error_t> fallible_function21_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function22_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function23_group27();

tl::expected<int, my_error_t> fallible_function22_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function23_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function24_group27();

tl::expected<int, my_error_t> fallible_function23_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function24_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function25_group27();

tl::expected<int, my_error_t> fallible_function24_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function25_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function26_group27();

tl::expected<int, my_error_t> fallible_function25_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function26_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function27_group27();

tl::expected<int, my_error_t> fallible_function26_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function27_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function28_group27();

tl::expected<int, my_error_t> fallible_function27_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function28_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function29_group27();

tl::expected<int, my_error_t> fallible_function28_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function29_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function30_group27();

tl::expected<int, my_error_t> fallible_function29_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function30_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function31_group27();

tl::expected<int, my_error_t> fallible_function30_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function31_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function32_group27();

tl::expected<int, my_error_t> fallible_function31_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function32_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function33_group27();

tl::expected<int, my_error_t> fallible_function32_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function33_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function34_group27();

tl::expected<int, my_error_t> fallible_function33_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function34_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function35_group27();

tl::expected<int, my_error_t> fallible_function34_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function35_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function36_group27();

tl::expected<int, my_error_t> fallible_function35_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function36_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function37_group27();

tl::expected<int, my_error_t> fallible_function36_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function37_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function38_group27();

tl::expected<int, my_error_t> fallible_function37_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function38_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function39_group27();

tl::expected<int, my_error_t> fallible_function38_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function39_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function40_group27();

tl::expected<int, my_error_t> fallible_function39_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function40_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function41_group27();

tl::expected<int, my_error_t> fallible_function40_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function41_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function42_group27();

tl::expected<int, my_error_t> fallible_function41_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function42_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function43_group27();

tl::expected<int, my_error_t> fallible_function42_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function43_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function44_group27();

tl::expected<int, my_error_t> fallible_function43_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function44_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function45_group27();

tl::expected<int, my_error_t> fallible_function44_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function45_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function46_group27();

tl::expected<int, my_error_t> fallible_function45_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function46_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function47_group27();

tl::expected<int, my_error_t> fallible_function46_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function47_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function48_group27();

tl::expected<int, my_error_t> fallible_function47_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function48_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function49_group27();

tl::expected<int, my_error_t> fallible_function48_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function49_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function50_group27();

tl::expected<int, my_error_t> fallible_function49_group27()
{
  auto result_a = non_trivial_destructor::initialize(27);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(27);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function50_group27(); result) {
    side_effect27 = side_effect27 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27;
}

tl::expected<int, my_error_t> fallible_function50_group27()
{
  if (auto result = non_trivial_destructor::initialize(50); result) {
    auto result_internal = result.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect27 + 1;
}

tl::expected<int, my_error_t> fallible_function1_group28();

tl::expected<int, my_error_t> fallible_function0_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function1_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function2_group28();

tl::expected<int, my_error_t> fallible_function1_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function2_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function3_group28();

tl::expected<int, my_error_t> fallible_function2_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function3_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function4_group28();

tl::expected<int, my_error_t> fallible_function3_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function4_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function5_group28();

tl::expected<int, my_error_t> fallible_function4_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function5_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function6_group28();

tl::expected<int, my_error_t> fallible_function5_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function6_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function7_group28();

tl::expected<int, my_error_t> fallible_function6_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function7_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function8_group28();

tl::expected<int, my_error_t> fallible_function7_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function8_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function9_group28();

tl::expected<int, my_error_t> fallible_function8_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function9_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function10_group28();

tl::expected<int, my_error_t> fallible_function9_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function10_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function11_group28();

tl::expected<int, my_error_t> fallible_function10_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function11_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function12_group28();

tl::expected<int, my_error_t> fallible_function11_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function12_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function13_group28();

tl::expected<int, my_error_t> fallible_function12_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function13_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function14_group28();

tl::expected<int, my_error_t> fallible_function13_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function14_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function15_group28();

tl::expected<int, my_error_t> fallible_function14_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function15_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function16_group28();

tl::expected<int, my_error_t> fallible_function15_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function16_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function17_group28();

tl::expected<int, my_error_t> fallible_function16_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function17_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function18_group28();

tl::expected<int, my_error_t> fallible_function17_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function18_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function19_group28();

tl::expected<int, my_error_t> fallible_function18_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function19_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function20_group28();

tl::expected<int, my_error_t> fallible_function19_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function20_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function21_group28();

tl::expected<int, my_error_t> fallible_function20_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function21_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function22_group28();

tl::expected<int, my_error_t> fallible_function21_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function22_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function23_group28();

tl::expected<int, my_error_t> fallible_function22_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function23_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function24_group28();

tl::expected<int, my_error_t> fallible_function23_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function24_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function25_group28();

tl::expected<int, my_error_t> fallible_function24_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function25_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function26_group28();

tl::expected<int, my_error_t> fallible_function25_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function26_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function27_group28();

tl::expected<int, my_error_t> fallible_function26_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function27_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function28_group28();

tl::expected<int, my_error_t> fallible_function27_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function28_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function29_group28();

tl::expected<int, my_error_t> fallible_function28_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function29_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function30_group28();

tl::expected<int, my_error_t> fallible_function29_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function30_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function31_group28();

tl::expected<int, my_error_t> fallible_function30_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function31_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function32_group28();

tl::expected<int, my_error_t> fallible_function31_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function32_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function33_group28();

tl::expected<int, my_error_t> fallible_function32_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function33_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function34_group28();

tl::expected<int, my_error_t> fallible_function33_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function34_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function35_group28();

tl::expected<int, my_error_t> fallible_function34_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function35_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function36_group28();

tl::expected<int, my_error_t> fallible_function35_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function36_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function37_group28();

tl::expected<int, my_error_t> fallible_function36_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function37_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function38_group28();

tl::expected<int, my_error_t> fallible_function37_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function38_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function39_group28();

tl::expected<int, my_error_t> fallible_function38_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function39_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function40_group28();

tl::expected<int, my_error_t> fallible_function39_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function40_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function41_group28();

tl::expected<int, my_error_t> fallible_function40_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function41_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function42_group28();

tl::expected<int, my_error_t> fallible_function41_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function42_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function43_group28();

tl::expected<int, my_error_t> fallible_function42_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function43_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function44_group28();

tl::expected<int, my_error_t> fallible_function43_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function44_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function45_group28();

tl::expected<int, my_error_t> fallible_function44_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function45_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function46_group28();

tl::expected<int, my_error_t> fallible_function45_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function46_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function47_group28();

tl::expected<int, my_error_t> fallible_function46_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function47_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function48_group28();

tl::expected<int, my_error_t> fallible_function47_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function48_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function49_group28();

tl::expected<int, my_error_t> fallible_function48_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function49_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function50_group28();

tl::expected<int, my_error_t> fallible_function49_group28()
{
  auto result_a = non_trivial_destructor::initialize(28);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(28);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function50_group28(); result) {
    side_effect28 = side_effect28 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28;
}

tl::expected<int, my_error_t> fallible_function50_group28()
{
  if (auto result = non_trivial_destructor::initialize(50); result) {
    auto result_internal = result.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect28 + 1;
}

tl::expected<int, my_error_t> fallible_function1_group29();

tl::expected<int, my_error_t> fallible_function0_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function1_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function2_group29();

tl::expected<int, my_error_t> fallible_function1_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function2_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function3_group29();

tl::expected<int, my_error_t> fallible_function2_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function3_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function4_group29();

tl::expected<int, my_error_t> fallible_function3_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function4_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function5_group29();

tl::expected<int, my_error_t> fallible_function4_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function5_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function6_group29();

tl::expected<int, my_error_t> fallible_function5_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function6_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function7_group29();

tl::expected<int, my_error_t> fallible_function6_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function7_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function8_group29();

tl::expected<int, my_error_t> fallible_function7_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function8_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function9_group29();

tl::expected<int, my_error_t> fallible_function8_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function9_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function10_group29();

tl::expected<int, my_error_t> fallible_function9_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function10_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function11_group29();

tl::expected<int, my_error_t> fallible_function10_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function11_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function12_group29();

tl::expected<int, my_error_t> fallible_function11_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function12_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function13_group29();

tl::expected<int, my_error_t> fallible_function12_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function13_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function14_group29();

tl::expected<int, my_error_t> fallible_function13_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function14_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function15_group29();

tl::expected<int, my_error_t> fallible_function14_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function15_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function16_group29();

tl::expected<int, my_error_t> fallible_function15_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function16_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function17_group29();

tl::expected<int, my_error_t> fallible_function16_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function17_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function18_group29();

tl::expected<int, my_error_t> fallible_function17_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function18_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function19_group29();

tl::expected<int, my_error_t> fallible_function18_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function19_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function20_group29();

tl::expected<int, my_error_t> fallible_function19_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function20_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function21_group29();

tl::expected<int, my_error_t> fallible_function20_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function21_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function22_group29();

tl::expected<int, my_error_t> fallible_function21_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function22_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function23_group29();

tl::expected<int, my_error_t> fallible_function22_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function23_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function24_group29();

tl::expected<int, my_error_t> fallible_function23_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function24_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function25_group29();

tl::expected<int, my_error_t> fallible_function24_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function25_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function26_group29();

tl::expected<int, my_error_t> fallible_function25_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function26_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function27_group29();

tl::expected<int, my_error_t> fallible_function26_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function27_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function28_group29();

tl::expected<int, my_error_t> fallible_function27_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function28_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function29_group29();

tl::expected<int, my_error_t> fallible_function28_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function29_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function30_group29();

tl::expected<int, my_error_t> fallible_function29_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function30_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function31_group29();

tl::expected<int, my_error_t> fallible_function30_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function31_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function32_group29();

tl::expected<int, my_error_t> fallible_function31_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function32_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function33_group29();

tl::expected<int, my_error_t> fallible_function32_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function33_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function34_group29();

tl::expected<int, my_error_t> fallible_function33_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function34_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function35_group29();

tl::expected<int, my_error_t> fallible_function34_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function35_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function36_group29();

tl::expected<int, my_error_t> fallible_function35_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function36_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function37_group29();

tl::expected<int, my_error_t> fallible_function36_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function37_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function38_group29();

tl::expected<int, my_error_t> fallible_function37_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function38_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function39_group29();

tl::expected<int, my_error_t> fallible_function38_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function39_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function40_group29();

tl::expected<int, my_error_t> fallible_function39_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function40_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function41_group29();

tl::expected<int, my_error_t> fallible_function40_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function41_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function42_group29();

tl::expected<int, my_error_t> fallible_function41_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function42_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function43_group29();

tl::expected<int, my_error_t> fallible_function42_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function43_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function44_group29();

tl::expected<int, my_error_t> fallible_function43_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function44_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function45_group29();

tl::expected<int, my_error_t> fallible_function44_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function45_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function46_group29();

tl::expected<int, my_error_t> fallible_function45_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function46_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function47_group29();

tl::expected<int, my_error_t> fallible_function46_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function47_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function48_group29();

tl::expected<int, my_error_t> fallible_function47_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function48_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function49_group29();

tl::expected<int, my_error_t> fallible_function48_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function49_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function50_group29();

tl::expected<int, my_error_t> fallible_function49_group29()
{
  auto result_a = non_trivial_destructor::initialize(29);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(29);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function50_group29(); result) {
    side_effect29 = side_effect29 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29;
}

tl::expected<int, my_error_t> fallible_function50_group29()
{
  if (auto result = non_trivial_destructor::initialize(50); result) {
    auto result_internal = result.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect29 + 1;
}

tl::expected<int, my_error_t> fallible_function1_group30();

tl::expected<int, my_error_t> fallible_function0_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function1_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function2_group30();

tl::expected<int, my_error_t> fallible_function1_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function2_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function3_group30();

tl::expected<int, my_error_t> fallible_function2_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function3_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function4_group30();

tl::expected<int, my_error_t> fallible_function3_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function4_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function5_group30();

tl::expected<int, my_error_t> fallible_function4_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function5_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function6_group30();

tl::expected<int, my_error_t> fallible_function5_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function6_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function7_group30();

tl::expected<int, my_error_t> fallible_function6_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function7_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function8_group30();

tl::expected<int, my_error_t> fallible_function7_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function8_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function9_group30();

tl::expected<int, my_error_t> fallible_function8_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function9_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function10_group30();

tl::expected<int, my_error_t> fallible_function9_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function10_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function11_group30();

tl::expected<int, my_error_t> fallible_function10_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function11_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function12_group30();

tl::expected<int, my_error_t> fallible_function11_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function12_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function13_group30();

tl::expected<int, my_error_t> fallible_function12_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function13_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function14_group30();

tl::expected<int, my_error_t> fallible_function13_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function14_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function15_group30();

tl::expected<int, my_error_t> fallible_function14_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function15_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function16_group30();

tl::expected<int, my_error_t> fallible_function15_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function16_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function17_group30();

tl::expected<int, my_error_t> fallible_function16_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function17_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function18_group30();

tl::expected<int, my_error_t> fallible_function17_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function18_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function19_group30();

tl::expected<int, my_error_t> fallible_function18_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function19_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function20_group30();

tl::expected<int, my_error_t> fallible_function19_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function20_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function21_group30();

tl::expected<int, my_error_t> fallible_function20_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function21_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function22_group30();

tl::expected<int, my_error_t> fallible_function21_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function22_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function23_group30();

tl::expected<int, my_error_t> fallible_function22_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function23_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function24_group30();

tl::expected<int, my_error_t> fallible_function23_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function24_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function25_group30();

tl::expected<int, my_error_t> fallible_function24_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function25_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function26_group30();

tl::expected<int, my_error_t> fallible_function25_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function26_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function27_group30();

tl::expected<int, my_error_t> fallible_function26_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function27_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function28_group30();

tl::expected<int, my_error_t> fallible_function27_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function28_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function29_group30();

tl::expected<int, my_error_t> fallible_function28_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function29_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function30_group30();

tl::expected<int, my_error_t> fallible_function29_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function30_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function31_group30();

tl::expected<int, my_error_t> fallible_function30_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function31_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function32_group30();

tl::expected<int, my_error_t> fallible_function31_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function32_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function33_group30();

tl::expected<int, my_error_t> fallible_function32_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function33_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function34_group30();

tl::expected<int, my_error_t> fallible_function33_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function34_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function35_group30();

tl::expected<int, my_error_t> fallible_function34_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function35_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function36_group30();

tl::expected<int, my_error_t> fallible_function35_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function36_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function37_group30();

tl::expected<int, my_error_t> fallible_function36_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function37_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function38_group30();

tl::expected<int, my_error_t> fallible_function37_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function38_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function39_group30();

tl::expected<int, my_error_t> fallible_function38_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function39_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function40_group30();

tl::expected<int, my_error_t> fallible_function39_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function40_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function41_group30();

tl::expected<int, my_error_t> fallible_function40_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function41_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function42_group30();

tl::expected<int, my_error_t> fallible_function41_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function42_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function43_group30();

tl::expected<int, my_error_t> fallible_function42_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function43_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function44_group30();

tl::expected<int, my_error_t> fallible_function43_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function44_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function45_group30();

tl::expected<int, my_error_t> fallible_function44_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function45_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function46_group30();

tl::expected<int, my_error_t> fallible_function45_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function46_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function47_group30();

tl::expected<int, my_error_t> fallible_function46_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function47_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function48_group30();

tl::expected<int, my_error_t> fallible_function47_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function48_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function49_group30();

tl::expected<int, my_error_t> fallible_function48_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function49_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function50_group30();

tl::expected<int, my_error_t> fallible_function49_group30()
{
  auto result_a = non_trivial_destructor::initialize(30);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(30);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function50_group30(); result) {
    side_effect30 = side_effect30 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30;
}

tl::expected<int, my_error_t> fallible_function50_group30()
{
  if (auto result = non_trivial_destructor::initialize(50); result) {
    auto result_internal = result.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect30 + 1;
}

tl::expected<int, my_error_t> fallible_function1_group31();

tl::expected<int, my_error_t> fallible_function0_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function1_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function2_group31();

tl::expected<int, my_error_t> fallible_function1_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function2_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function3_group31();

tl::expected<int, my_error_t> fallible_function2_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function3_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function4_group31();

tl::expected<int, my_error_t> fallible_function3_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function4_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function5_group31();

tl::expected<int, my_error_t> fallible_function4_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function5_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function6_group31();

tl::expected<int, my_error_t> fallible_function5_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function6_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function7_group31();

tl::expected<int, my_error_t> fallible_function6_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function7_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function8_group31();

tl::expected<int, my_error_t> fallible_function7_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function8_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function9_group31();

tl::expected<int, my_error_t> fallible_function8_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function9_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function10_group31();

tl::expected<int, my_error_t> fallible_function9_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function10_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function11_group31();

tl::expected<int, my_error_t> fallible_function10_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function11_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function12_group31();

tl::expected<int, my_error_t> fallible_function11_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function12_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function13_group31();

tl::expected<int, my_error_t> fallible_function12_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function13_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function14_group31();

tl::expected<int, my_error_t> fallible_function13_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function14_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function15_group31();

tl::expected<int, my_error_t> fallible_function14_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function15_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function16_group31();

tl::expected<int, my_error_t> fallible_function15_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function16_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function17_group31();

tl::expected<int, my_error_t> fallible_function16_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function17_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function18_group31();

tl::expected<int, my_error_t> fallible_function17_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function18_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function19_group31();

tl::expected<int, my_error_t> fallible_function18_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function19_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function20_group31();

tl::expected<int, my_error_t> fallible_function19_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function20_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function21_group31();

tl::expected<int, my_error_t> fallible_function20_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function21_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function22_group31();

tl::expected<int, my_error_t> fallible_function21_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function22_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function23_group31();

tl::expected<int, my_error_t> fallible_function22_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function23_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function24_group31();

tl::expected<int, my_error_t> fallible_function23_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function24_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function25_group31();

tl::expected<int, my_error_t> fallible_function24_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function25_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function26_group31();

tl::expected<int, my_error_t> fallible_function25_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function26_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function27_group31();

tl::expected<int, my_error_t> fallible_function26_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function27_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function28_group31();

tl::expected<int, my_error_t> fallible_function27_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function28_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function29_group31();

tl::expected<int, my_error_t> fallible_function28_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function29_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function30_group31();

tl::expected<int, my_error_t> fallible_function29_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function30_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function31_group31();

tl::expected<int, my_error_t> fallible_function30_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function31_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function32_group31();

tl::expected<int, my_error_t> fallible_function31_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function32_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function33_group31();

tl::expected<int, my_error_t> fallible_function32_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function33_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function34_group31();

tl::expected<int, my_error_t> fallible_function33_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function34_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function35_group31();

tl::expected<int, my_error_t> fallible_function34_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function35_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function36_group31();

tl::expected<int, my_error_t> fallible_function35_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function36_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function37_group31();

tl::expected<int, my_error_t> fallible_function36_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function37_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function38_group31();

tl::expected<int, my_error_t> fallible_function37_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function38_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function39_group31();

tl::expected<int, my_error_t> fallible_function38_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function39_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function40_group31();

tl::expected<int, my_error_t> fallible_function39_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function40_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function41_group31();

tl::expected<int, my_error_t> fallible_function40_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function41_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function42_group31();

tl::expected<int, my_error_t> fallible_function41_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function42_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function43_group31();

tl::expected<int, my_error_t> fallible_function42_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function43_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function44_group31();

tl::expected<int, my_error_t> fallible_function43_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function44_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function45_group31();

tl::expected<int, my_error_t> fallible_function44_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function45_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function46_group31();

tl::expected<int, my_error_t> fallible_function45_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function46_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function47_group31();

tl::expected<int, my_error_t> fallible_function46_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function47_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function48_group31();

tl::expected<int, my_error_t> fallible_function47_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function48_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function49_group31();

tl::expected<int, my_error_t> fallible_function48_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function49_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function50_group31();

tl::expected<int, my_error_t> fallible_function49_group31()
{
  auto result_a = non_trivial_destructor::initialize(31);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(31);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function50_group31(); result) {
    side_effect31 = side_effect31 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31;
}

tl::expected<int, my_error_t> fallible_function50_group31()
{
  if (auto result = non_trivial_destructor::initialize(50); result) {
    auto result_internal = result.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect31 + 1;
}

tl::expected<int, my_error_t> fallible_function1_group32();

tl::expected<int, my_error_t> fallible_function0_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function1_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function2_group32();

tl::expected<int, my_error_t> fallible_function1_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function2_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function3_group32();

tl::expected<int, my_error_t> fallible_function2_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function3_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function4_group32();

tl::expected<int, my_error_t> fallible_function3_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function4_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function5_group32();

tl::expected<int, my_error_t> fallible_function4_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function5_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function6_group32();

tl::expected<int, my_error_t> fallible_function5_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function6_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function7_group32();

tl::expected<int, my_error_t> fallible_function6_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function7_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function8_group32();

tl::expected<int, my_error_t> fallible_function7_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function8_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function9_group32();

tl::expected<int, my_error_t> fallible_function8_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function9_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function10_group32();

tl::expected<int, my_error_t> fallible_function9_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function10_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function11_group32();

tl::expected<int, my_error_t> fallible_function10_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function11_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function12_group32();

tl::expected<int, my_error_t> fallible_function11_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function12_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function13_group32();

tl::expected<int, my_error_t> fallible_function12_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function13_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function14_group32();

tl::expected<int, my_error_t> fallible_function13_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function14_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function15_group32();

tl::expected<int, my_error_t> fallible_function14_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function15_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function16_group32();

tl::expected<int, my_error_t> fallible_function15_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function16_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function17_group32();

tl::expected<int, my_error_t> fallible_function16_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function17_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function18_group32();

tl::expected<int, my_error_t> fallible_function17_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function18_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function19_group32();

tl::expected<int, my_error_t> fallible_function18_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function19_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function20_group32();

tl::expected<int, my_error_t> fallible_function19_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function20_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function21_group32();

tl::expected<int, my_error_t> fallible_function20_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function21_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function22_group32();

tl::expected<int, my_error_t> fallible_function21_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function22_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function23_group32();

tl::expected<int, my_error_t> fallible_function22_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function23_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function24_group32();

tl::expected<int, my_error_t> fallible_function23_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function24_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function25_group32();

tl::expected<int, my_error_t> fallible_function24_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function25_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function26_group32();

tl::expected<int, my_error_t> fallible_function25_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function26_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function27_group32();

tl::expected<int, my_error_t> fallible_function26_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function27_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function28_group32();

tl::expected<int, my_error_t> fallible_function27_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function28_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function29_group32();

tl::expected<int, my_error_t> fallible_function28_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function29_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function30_group32();

tl::expected<int, my_error_t> fallible_function29_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function30_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function31_group32();

tl::expected<int, my_error_t> fallible_function30_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function31_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function32_group32();

tl::expected<int, my_error_t> fallible_function31_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function32_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function33_group32();

tl::expected<int, my_error_t> fallible_function32_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function33_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function34_group32();

tl::expected<int, my_error_t> fallible_function33_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function34_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function35_group32();

tl::expected<int, my_error_t> fallible_function34_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function35_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function36_group32();

tl::expected<int, my_error_t> fallible_function35_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function36_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function37_group32();

tl::expected<int, my_error_t> fallible_function36_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function37_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function38_group32();

tl::expected<int, my_error_t> fallible_function37_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function38_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function39_group32();

tl::expected<int, my_error_t> fallible_function38_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function39_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function40_group32();

tl::expected<int, my_error_t> fallible_function39_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function40_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function41_group32();

tl::expected<int, my_error_t> fallible_function40_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function41_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function42_group32();

tl::expected<int, my_error_t> fallible_function41_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function42_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function43_group32();

tl::expected<int, my_error_t> fallible_function42_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function43_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function44_group32();

tl::expected<int, my_error_t> fallible_function43_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function44_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function45_group32();

tl::expected<int, my_error_t> fallible_function44_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function45_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function46_group32();

tl::expected<int, my_error_t> fallible_function45_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function46_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function47_group32();

tl::expected<int, my_error_t> fallible_function46_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function47_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function48_group32();

tl::expected<int, my_error_t> fallible_function47_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function48_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function49_group32();

tl::expected<int, my_error_t> fallible_function48_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function49_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function50_group32();

tl::expected<int, my_error_t> fallible_function49_group32()
{
  auto result_a = non_trivial_destructor::initialize(32);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(32);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function50_group32(); result) {
    side_effect32 = side_effect32 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32;
}

tl::expected<int, my_error_t> fallible_function50_group32()
{
  if (auto result = non_trivial_destructor::initialize(50); result) {
    auto result_internal = result.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect32 + 1;
}

tl::expected<int, my_error_t> fallible_function1_group33();

tl::expected<int, my_error_t> fallible_function0_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function1_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function2_group33();

tl::expected<int, my_error_t> fallible_function1_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function2_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function3_group33();

tl::expected<int, my_error_t> fallible_function2_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function3_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function4_group33();

tl::expected<int, my_error_t> fallible_function3_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function4_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function5_group33();

tl::expected<int, my_error_t> fallible_function4_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function5_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function6_group33();

tl::expected<int, my_error_t> fallible_function5_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function6_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function7_group33();

tl::expected<int, my_error_t> fallible_function6_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function7_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function8_group33();

tl::expected<int, my_error_t> fallible_function7_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function8_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function9_group33();

tl::expected<int, my_error_t> fallible_function8_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function9_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function10_group33();

tl::expected<int, my_error_t> fallible_function9_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function10_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function11_group33();

tl::expected<int, my_error_t> fallible_function10_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function11_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function12_group33();

tl::expected<int, my_error_t> fallible_function11_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function12_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function13_group33();

tl::expected<int, my_error_t> fallible_function12_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function13_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function14_group33();

tl::expected<int, my_error_t> fallible_function13_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function14_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function15_group33();

tl::expected<int, my_error_t> fallible_function14_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function15_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function16_group33();

tl::expected<int, my_error_t> fallible_function15_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function16_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function17_group33();

tl::expected<int, my_error_t> fallible_function16_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function17_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function18_group33();

tl::expected<int, my_error_t> fallible_function17_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function18_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function19_group33();

tl::expected<int, my_error_t> fallible_function18_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function19_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function20_group33();

tl::expected<int, my_error_t> fallible_function19_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function20_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function21_group33();

tl::expected<int, my_error_t> fallible_function20_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function21_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function22_group33();

tl::expected<int, my_error_t> fallible_function21_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function22_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function23_group33();

tl::expected<int, my_error_t> fallible_function22_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function23_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function24_group33();

tl::expected<int, my_error_t> fallible_function23_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function24_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function25_group33();

tl::expected<int, my_error_t> fallible_function24_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function25_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function26_group33();

tl::expected<int, my_error_t> fallible_function25_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function26_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function27_group33();

tl::expected<int, my_error_t> fallible_function26_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function27_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function28_group33();

tl::expected<int, my_error_t> fallible_function27_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function28_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function29_group33();

tl::expected<int, my_error_t> fallible_function28_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function29_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function30_group33();

tl::expected<int, my_error_t> fallible_function29_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function30_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function31_group33();

tl::expected<int, my_error_t> fallible_function30_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function31_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function32_group33();

tl::expected<int, my_error_t> fallible_function31_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function32_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function33_group33();

tl::expected<int, my_error_t> fallible_function32_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function33_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function34_group33();

tl::expected<int, my_error_t> fallible_function33_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function34_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function35_group33();

tl::expected<int, my_error_t> fallible_function34_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function35_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function36_group33();

tl::expected<int, my_error_t> fallible_function35_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function36_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function37_group33();

tl::expected<int, my_error_t> fallible_function36_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function37_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function38_group33();

tl::expected<int, my_error_t> fallible_function37_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function38_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function39_group33();

tl::expected<int, my_error_t> fallible_function38_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function39_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function40_group33();

tl::expected<int, my_error_t> fallible_function39_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function40_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function41_group33();

tl::expected<int, my_error_t> fallible_function40_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function41_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function42_group33();

tl::expected<int, my_error_t> fallible_function41_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function42_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function43_group33();

tl::expected<int, my_error_t> fallible_function42_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function43_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function44_group33();

tl::expected<int, my_error_t> fallible_function43_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function44_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function45_group33();

tl::expected<int, my_error_t> fallible_function44_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function45_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function46_group33();

tl::expected<int, my_error_t> fallible_function45_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function46_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function47_group33();

tl::expected<int, my_error_t> fallible_function46_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function47_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function48_group33();

tl::expected<int, my_error_t> fallible_function47_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function48_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function49_group33();

tl::expected<int, my_error_t> fallible_function48_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function49_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function50_group33();

tl::expected<int, my_error_t> fallible_function49_group33()
{
  auto result_a = non_trivial_destructor::initialize(33);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(33);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function50_group33(); result) {
    side_effect33 = side_effect33 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33;
}

tl::expected<int, my_error_t> fallible_function50_group33()
{
  if (auto result = non_trivial_destructor::initialize(50); result) {
    auto result_internal = result.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect33 + 1;
}

tl::expected<int, my_error_t> fallible_function1_group34();

tl::expected<int, my_error_t> fallible_function0_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function1_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function2_group34();

tl::expected<int, my_error_t> fallible_function1_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function2_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function3_group34();

tl::expected<int, my_error_t> fallible_function2_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function3_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function4_group34();

tl::expected<int, my_error_t> fallible_function3_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function4_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function5_group34();

tl::expected<int, my_error_t> fallible_function4_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function5_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function6_group34();

tl::expected<int, my_error_t> fallible_function5_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function6_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function7_group34();

tl::expected<int, my_error_t> fallible_function6_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function7_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function8_group34();

tl::expected<int, my_error_t> fallible_function7_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function8_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function9_group34();

tl::expected<int, my_error_t> fallible_function8_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function9_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function10_group34();

tl::expected<int, my_error_t> fallible_function9_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function10_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function11_group34();

tl::expected<int, my_error_t> fallible_function10_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function11_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function12_group34();

tl::expected<int, my_error_t> fallible_function11_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function12_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function13_group34();

tl::expected<int, my_error_t> fallible_function12_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function13_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function14_group34();

tl::expected<int, my_error_t> fallible_function13_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function14_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function15_group34();

tl::expected<int, my_error_t> fallible_function14_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function15_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function16_group34();

tl::expected<int, my_error_t> fallible_function15_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function16_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function17_group34();

tl::expected<int, my_error_t> fallible_function16_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function17_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function18_group34();

tl::expected<int, my_error_t> fallible_function17_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function18_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function19_group34();

tl::expected<int, my_error_t> fallible_function18_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function19_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function20_group34();

tl::expected<int, my_error_t> fallible_function19_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function20_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function21_group34();

tl::expected<int, my_error_t> fallible_function20_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function21_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function22_group34();

tl::expected<int, my_error_t> fallible_function21_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function22_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function23_group34();

tl::expected<int, my_error_t> fallible_function22_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function23_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function24_group34();

tl::expected<int, my_error_t> fallible_function23_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function24_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function25_group34();

tl::expected<int, my_error_t> fallible_function24_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function25_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function26_group34();

tl::expected<int, my_error_t> fallible_function25_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function26_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function27_group34();

tl::expected<int, my_error_t> fallible_function26_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function27_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function28_group34();

tl::expected<int, my_error_t> fallible_function27_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function28_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function29_group34();

tl::expected<int, my_error_t> fallible_function28_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function29_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function30_group34();

tl::expected<int, my_error_t> fallible_function29_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function30_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function31_group34();

tl::expected<int, my_error_t> fallible_function30_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function31_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function32_group34();

tl::expected<int, my_error_t> fallible_function31_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function32_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function33_group34();

tl::expected<int, my_error_t> fallible_function32_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function33_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function34_group34();

tl::expected<int, my_error_t> fallible_function33_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function34_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function35_group34();

tl::expected<int, my_error_t> fallible_function34_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function35_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function36_group34();

tl::expected<int, my_error_t> fallible_function35_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function36_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function37_group34();

tl::expected<int, my_error_t> fallible_function36_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function37_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function38_group34();

tl::expected<int, my_error_t> fallible_function37_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function38_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function39_group34();

tl::expected<int, my_error_t> fallible_function38_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function39_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function40_group34();

tl::expected<int, my_error_t> fallible_function39_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function40_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function41_group34();

tl::expected<int, my_error_t> fallible_function40_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function41_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function42_group34();

tl::expected<int, my_error_t> fallible_function41_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function42_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function43_group34();

tl::expected<int, my_error_t> fallible_function42_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function43_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function44_group34();

tl::expected<int, my_error_t> fallible_function43_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function44_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function45_group34();

tl::expected<int, my_error_t> fallible_function44_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function45_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function46_group34();

tl::expected<int, my_error_t> fallible_function45_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function46_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function47_group34();

tl::expected<int, my_error_t> fallible_function46_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function47_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function48_group34();

tl::expected<int, my_error_t> fallible_function47_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function48_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function49_group34();

tl::expected<int, my_error_t> fallible_function48_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function49_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function50_group34();

tl::expected<int, my_error_t> fallible_function49_group34()
{
  auto result_a = non_trivial_destructor::initialize(34);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(34);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function50_group34(); result) {
    side_effect34 = side_effect34 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34;
}

tl::expected<int, my_error_t> fallible_function50_group34()
{
  if (auto result = non_trivial_destructor::initialize(50); result) {
    auto result_internal = result.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect34 + 1;
}

tl::expected<int, my_error_t> fallible_function1_group35();

tl::expected<int, my_error_t> fallible_function0_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function1_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function2_group35();

tl::expected<int, my_error_t> fallible_function1_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function2_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function3_group35();

tl::expected<int, my_error_t> fallible_function2_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function3_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function4_group35();

tl::expected<int, my_error_t> fallible_function3_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function4_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function5_group35();

tl::expected<int, my_error_t> fallible_function4_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function5_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function6_group35();

tl::expected<int, my_error_t> fallible_function5_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function6_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function7_group35();

tl::expected<int, my_error_t> fallible_function6_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function7_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function8_group35();

tl::expected<int, my_error_t> fallible_function7_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function8_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function9_group35();

tl::expected<int, my_error_t> fallible_function8_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function9_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function10_group35();

tl::expected<int, my_error_t> fallible_function9_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function10_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function11_group35();

tl::expected<int, my_error_t> fallible_function10_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function11_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function12_group35();

tl::expected<int, my_error_t> fallible_function11_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function12_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function13_group35();

tl::expected<int, my_error_t> fallible_function12_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function13_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function14_group35();

tl::expected<int, my_error_t> fallible_function13_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function14_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function15_group35();

tl::expected<int, my_error_t> fallible_function14_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function15_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function16_group35();

tl::expected<int, my_error_t> fallible_function15_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function16_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function17_group35();

tl::expected<int, my_error_t> fallible_function16_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function17_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function18_group35();

tl::expected<int, my_error_t> fallible_function17_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function18_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function19_group35();

tl::expected<int, my_error_t> fallible_function18_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function19_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function20_group35();

tl::expected<int, my_error_t> fallible_function19_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function20_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function21_group35();

tl::expected<int, my_error_t> fallible_function20_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function21_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function22_group35();

tl::expected<int, my_error_t> fallible_function21_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function22_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function23_group35();

tl::expected<int, my_error_t> fallible_function22_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function23_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function24_group35();

tl::expected<int, my_error_t> fallible_function23_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function24_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function25_group35();

tl::expected<int, my_error_t> fallible_function24_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function25_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function26_group35();

tl::expected<int, my_error_t> fallible_function25_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function26_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function27_group35();

tl::expected<int, my_error_t> fallible_function26_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function27_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function28_group35();

tl::expected<int, my_error_t> fallible_function27_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function28_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function29_group35();

tl::expected<int, my_error_t> fallible_function28_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function29_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function30_group35();

tl::expected<int, my_error_t> fallible_function29_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function30_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function31_group35();

tl::expected<int, my_error_t> fallible_function30_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function31_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function32_group35();

tl::expected<int, my_error_t> fallible_function31_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function32_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function33_group35();

tl::expected<int, my_error_t> fallible_function32_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function33_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function34_group35();

tl::expected<int, my_error_t> fallible_function33_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function34_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function35_group35();

tl::expected<int, my_error_t> fallible_function34_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function35_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function36_group35();

tl::expected<int, my_error_t> fallible_function35_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function36_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function37_group35();

tl::expected<int, my_error_t> fallible_function36_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function37_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function38_group35();

tl::expected<int, my_error_t> fallible_function37_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function38_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function39_group35();

tl::expected<int, my_error_t> fallible_function38_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function39_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function40_group35();

tl::expected<int, my_error_t> fallible_function39_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function40_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function41_group35();

tl::expected<int, my_error_t> fallible_function40_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function41_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function42_group35();

tl::expected<int, my_error_t> fallible_function41_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function42_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function43_group35();

tl::expected<int, my_error_t> fallible_function42_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function43_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function44_group35();

tl::expected<int, my_error_t> fallible_function43_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function44_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function45_group35();

tl::expected<int, my_error_t> fallible_function44_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function45_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function46_group35();

tl::expected<int, my_error_t> fallible_function45_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function46_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function47_group35();

tl::expected<int, my_error_t> fallible_function46_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function47_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function48_group35();

tl::expected<int, my_error_t> fallible_function47_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function48_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function49_group35();

tl::expected<int, my_error_t> fallible_function48_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function49_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function50_group35();

tl::expected<int, my_error_t> fallible_function49_group35()
{
  auto result_a = non_trivial_destructor::initialize(35);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(35);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function50_group35(); result) {
    side_effect35 = side_effect35 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35;
}

tl::expected<int, my_error_t> fallible_function50_group35()
{
  if (auto result = non_trivial_destructor::initialize(50); result) {
    auto result_internal = result.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect35 + 1;
}

tl::expected<int, my_error_t> fallible_function1_group36();

tl::expected<int, my_error_t> fallible_function0_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function1_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function2_group36();

tl::expected<int, my_error_t> fallible_function1_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function2_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function3_group36();

tl::expected<int, my_error_t> fallible_function2_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function3_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function4_group36();

tl::expected<int, my_error_t> fallible_function3_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function4_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function5_group36();

tl::expected<int, my_error_t> fallible_function4_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function5_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function6_group36();

tl::expected<int, my_error_t> fallible_function5_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function6_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function7_group36();

tl::expected<int, my_error_t> fallible_function6_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function7_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function8_group36();

tl::expected<int, my_error_t> fallible_function7_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function8_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function9_group36();

tl::expected<int, my_error_t> fallible_function8_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function9_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function10_group36();

tl::expected<int, my_error_t> fallible_function9_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function10_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function11_group36();

tl::expected<int, my_error_t> fallible_function10_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function11_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function12_group36();

tl::expected<int, my_error_t> fallible_function11_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function12_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function13_group36();

tl::expected<int, my_error_t> fallible_function12_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function13_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function14_group36();

tl::expected<int, my_error_t> fallible_function13_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function14_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function15_group36();

tl::expected<int, my_error_t> fallible_function14_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function15_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function16_group36();

tl::expected<int, my_error_t> fallible_function15_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function16_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function17_group36();

tl::expected<int, my_error_t> fallible_function16_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function17_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function18_group36();

tl::expected<int, my_error_t> fallible_function17_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function18_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function19_group36();

tl::expected<int, my_error_t> fallible_function18_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function19_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function20_group36();

tl::expected<int, my_error_t> fallible_function19_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function20_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function21_group36();

tl::expected<int, my_error_t> fallible_function20_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function21_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function22_group36();

tl::expected<int, my_error_t> fallible_function21_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function22_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function23_group36();

tl::expected<int, my_error_t> fallible_function22_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function23_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function24_group36();

tl::expected<int, my_error_t> fallible_function23_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function24_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function25_group36();

tl::expected<int, my_error_t> fallible_function24_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function25_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function26_group36();

tl::expected<int, my_error_t> fallible_function25_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function26_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function27_group36();

tl::expected<int, my_error_t> fallible_function26_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function27_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function28_group36();

tl::expected<int, my_error_t> fallible_function27_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function28_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function29_group36();

tl::expected<int, my_error_t> fallible_function28_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function29_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function30_group36();

tl::expected<int, my_error_t> fallible_function29_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function30_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function31_group36();

tl::expected<int, my_error_t> fallible_function30_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function31_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function32_group36();

tl::expected<int, my_error_t> fallible_function31_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function32_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function33_group36();

tl::expected<int, my_error_t> fallible_function32_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function33_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function34_group36();

tl::expected<int, my_error_t> fallible_function33_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function34_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function35_group36();

tl::expected<int, my_error_t> fallible_function34_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function35_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function36_group36();

tl::expected<int, my_error_t> fallible_function35_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function36_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function37_group36();

tl::expected<int, my_error_t> fallible_function36_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function37_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function38_group36();

tl::expected<int, my_error_t> fallible_function37_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function38_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function39_group36();

tl::expected<int, my_error_t> fallible_function38_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function39_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function40_group36();

tl::expected<int, my_error_t> fallible_function39_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function40_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function41_group36();

tl::expected<int, my_error_t> fallible_function40_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function41_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function42_group36();

tl::expected<int, my_error_t> fallible_function41_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function42_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function43_group36();

tl::expected<int, my_error_t> fallible_function42_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function43_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function44_group36();

tl::expected<int, my_error_t> fallible_function43_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function44_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function45_group36();

tl::expected<int, my_error_t> fallible_function44_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function45_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function46_group36();

tl::expected<int, my_error_t> fallible_function45_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function46_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function47_group36();

tl::expected<int, my_error_t> fallible_function46_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function47_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function48_group36();

tl::expected<int, my_error_t> fallible_function47_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function48_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function49_group36();

tl::expected<int, my_error_t> fallible_function48_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function49_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function50_group36();

tl::expected<int, my_error_t> fallible_function49_group36()
{
  auto result_a = non_trivial_destructor::initialize(36);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(36);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function50_group36(); result) {
    side_effect36 = side_effect36 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36;
}

tl::expected<int, my_error_t> fallible_function50_group36()
{
  if (auto result = non_trivial_destructor::initialize(50); result) {
    auto result_internal = result.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect36 + 1;
}

tl::expected<int, my_error_t> fallible_function1_group37();

tl::expected<int, my_error_t> fallible_function0_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function1_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function2_group37();

tl::expected<int, my_error_t> fallible_function1_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function2_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function3_group37();

tl::expected<int, my_error_t> fallible_function2_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function3_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function4_group37();

tl::expected<int, my_error_t> fallible_function3_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function4_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function5_group37();

tl::expected<int, my_error_t> fallible_function4_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function5_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function6_group37();

tl::expected<int, my_error_t> fallible_function5_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function6_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function7_group37();

tl::expected<int, my_error_t> fallible_function6_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function7_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function8_group37();

tl::expected<int, my_error_t> fallible_function7_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function8_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function9_group37();

tl::expected<int, my_error_t> fallible_function8_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function9_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function10_group37();

tl::expected<int, my_error_t> fallible_function9_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function10_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function11_group37();

tl::expected<int, my_error_t> fallible_function10_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function11_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function12_group37();

tl::expected<int, my_error_t> fallible_function11_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function12_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function13_group37();

tl::expected<int, my_error_t> fallible_function12_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function13_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function14_group37();

tl::expected<int, my_error_t> fallible_function13_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function14_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function15_group37();

tl::expected<int, my_error_t> fallible_function14_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function15_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function16_group37();

tl::expected<int, my_error_t> fallible_function15_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function16_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function17_group37();

tl::expected<int, my_error_t> fallible_function16_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function17_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function18_group37();

tl::expected<int, my_error_t> fallible_function17_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function18_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function19_group37();

tl::expected<int, my_error_t> fallible_function18_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function19_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function20_group37();

tl::expected<int, my_error_t> fallible_function19_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function20_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function21_group37();

tl::expected<int, my_error_t> fallible_function20_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function21_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function22_group37();

tl::expected<int, my_error_t> fallible_function21_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function22_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function23_group37();

tl::expected<int, my_error_t> fallible_function22_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function23_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function24_group37();

tl::expected<int, my_error_t> fallible_function23_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function24_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function25_group37();

tl::expected<int, my_error_t> fallible_function24_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function25_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function26_group37();

tl::expected<int, my_error_t> fallible_function25_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function26_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function27_group37();

tl::expected<int, my_error_t> fallible_function26_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function27_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function28_group37();

tl::expected<int, my_error_t> fallible_function27_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function28_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function29_group37();

tl::expected<int, my_error_t> fallible_function28_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function29_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function30_group37();

tl::expected<int, my_error_t> fallible_function29_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function30_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function31_group37();

tl::expected<int, my_error_t> fallible_function30_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function31_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function32_group37();

tl::expected<int, my_error_t> fallible_function31_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function32_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function33_group37();

tl::expected<int, my_error_t> fallible_function32_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function33_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function34_group37();

tl::expected<int, my_error_t> fallible_function33_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function34_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function35_group37();

tl::expected<int, my_error_t> fallible_function34_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function35_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function36_group37();

tl::expected<int, my_error_t> fallible_function35_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function36_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function37_group37();

tl::expected<int, my_error_t> fallible_function36_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function37_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function38_group37();

tl::expected<int, my_error_t> fallible_function37_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function38_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function39_group37();

tl::expected<int, my_error_t> fallible_function38_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function39_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function40_group37();

tl::expected<int, my_error_t> fallible_function39_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function40_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function41_group37();

tl::expected<int, my_error_t> fallible_function40_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function41_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function42_group37();

tl::expected<int, my_error_t> fallible_function41_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function42_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function43_group37();

tl::expected<int, my_error_t> fallible_function42_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function43_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function44_group37();

tl::expected<int, my_error_t> fallible_function43_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function44_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function45_group37();

tl::expected<int, my_error_t> fallible_function44_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function45_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function46_group37();

tl::expected<int, my_error_t> fallible_function45_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function46_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function47_group37();

tl::expected<int, my_error_t> fallible_function46_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function47_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function48_group37();

tl::expected<int, my_error_t> fallible_function47_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function48_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function49_group37();

tl::expected<int, my_error_t> fallible_function48_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function49_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function50_group37();

tl::expected<int, my_error_t> fallible_function49_group37()
{
  auto result_a = non_trivial_destructor::initialize(37);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(37);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function50_group37(); result) {
    side_effect37 = side_effect37 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37;
}

tl::expected<int, my_error_t> fallible_function50_group37()
{
  if (auto result = non_trivial_destructor::initialize(50); result) {
    auto result_internal = result.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect37 + 1;
}

tl::expected<int, my_error_t> fallible_function1_group38();

tl::expected<int, my_error_t> fallible_function0_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function1_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function2_group38();

tl::expected<int, my_error_t> fallible_function1_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function2_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function3_group38();

tl::expected<int, my_error_t> fallible_function2_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function3_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function4_group38();

tl::expected<int, my_error_t> fallible_function3_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function4_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function5_group38();

tl::expected<int, my_error_t> fallible_function4_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function5_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function6_group38();

tl::expected<int, my_error_t> fallible_function5_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function6_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function7_group38();

tl::expected<int, my_error_t> fallible_function6_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function7_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function8_group38();

tl::expected<int, my_error_t> fallible_function7_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function8_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function9_group38();

tl::expected<int, my_error_t> fallible_function8_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function9_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function10_group38();

tl::expected<int, my_error_t> fallible_function9_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function10_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function11_group38();

tl::expected<int, my_error_t> fallible_function10_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function11_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function12_group38();

tl::expected<int, my_error_t> fallible_function11_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function12_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function13_group38();

tl::expected<int, my_error_t> fallible_function12_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function13_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function14_group38();

tl::expected<int, my_error_t> fallible_function13_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function14_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function15_group38();

tl::expected<int, my_error_t> fallible_function14_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function15_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function16_group38();

tl::expected<int, my_error_t> fallible_function15_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function16_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function17_group38();

tl::expected<int, my_error_t> fallible_function16_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function17_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function18_group38();

tl::expected<int, my_error_t> fallible_function17_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function18_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function19_group38();

tl::expected<int, my_error_t> fallible_function18_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function19_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function20_group38();

tl::expected<int, my_error_t> fallible_function19_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function20_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function21_group38();

tl::expected<int, my_error_t> fallible_function20_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function21_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function22_group38();

tl::expected<int, my_error_t> fallible_function21_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function22_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function23_group38();

tl::expected<int, my_error_t> fallible_function22_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function23_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function24_group38();

tl::expected<int, my_error_t> fallible_function23_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function24_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function25_group38();

tl::expected<int, my_error_t> fallible_function24_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function25_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function26_group38();

tl::expected<int, my_error_t> fallible_function25_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function26_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function27_group38();

tl::expected<int, my_error_t> fallible_function26_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function27_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function28_group38();

tl::expected<int, my_error_t> fallible_function27_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function28_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function29_group38();

tl::expected<int, my_error_t> fallible_function28_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function29_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function30_group38();

tl::expected<int, my_error_t> fallible_function29_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function30_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function31_group38();

tl::expected<int, my_error_t> fallible_function30_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function31_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function32_group38();

tl::expected<int, my_error_t> fallible_function31_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function32_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function33_group38();

tl::expected<int, my_error_t> fallible_function32_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function33_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function34_group38();

tl::expected<int, my_error_t> fallible_function33_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function34_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function35_group38();

tl::expected<int, my_error_t> fallible_function34_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function35_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function36_group38();

tl::expected<int, my_error_t> fallible_function35_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function36_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function37_group38();

tl::expected<int, my_error_t> fallible_function36_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function37_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function38_group38();

tl::expected<int, my_error_t> fallible_function37_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function38_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function39_group38();

tl::expected<int, my_error_t> fallible_function38_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function39_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function40_group38();

tl::expected<int, my_error_t> fallible_function39_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function40_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function41_group38();

tl::expected<int, my_error_t> fallible_function40_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function41_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function42_group38();

tl::expected<int, my_error_t> fallible_function41_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function42_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function43_group38();

tl::expected<int, my_error_t> fallible_function42_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function43_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function44_group38();

tl::expected<int, my_error_t> fallible_function43_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function44_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function45_group38();

tl::expected<int, my_error_t> fallible_function44_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function45_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function46_group38();

tl::expected<int, my_error_t> fallible_function45_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function46_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function47_group38();

tl::expected<int, my_error_t> fallible_function46_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function47_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function48_group38();

tl::expected<int, my_error_t> fallible_function47_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function48_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function49_group38();

tl::expected<int, my_error_t> fallible_function48_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function49_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function50_group38();

tl::expected<int, my_error_t> fallible_function49_group38()
{
  auto result_a = non_trivial_destructor::initialize(38);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(38);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function50_group38(); result) {
    side_effect38 = side_effect38 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38;
}

tl::expected<int, my_error_t> fallible_function50_group38()
{
  if (auto result = non_trivial_destructor::initialize(50); result) {
    auto result_internal = result.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect38 + 1;
}

tl::expected<int, my_error_t> fallible_function1_group39();

tl::expected<int, my_error_t> fallible_function0_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function1_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function2_group39();

tl::expected<int, my_error_t> fallible_function1_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function2_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function3_group39();

tl::expected<int, my_error_t> fallible_function2_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function3_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function4_group39();

tl::expected<int, my_error_t> fallible_function3_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function4_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function5_group39();

tl::expected<int, my_error_t> fallible_function4_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function5_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function6_group39();

tl::expected<int, my_error_t> fallible_function5_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function6_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function7_group39();

tl::expected<int, my_error_t> fallible_function6_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function7_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function8_group39();

tl::expected<int, my_error_t> fallible_function7_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function8_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function9_group39();

tl::expected<int, my_error_t> fallible_function8_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function9_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function10_group39();

tl::expected<int, my_error_t> fallible_function9_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function10_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function11_group39();

tl::expected<int, my_error_t> fallible_function10_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function11_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function12_group39();

tl::expected<int, my_error_t> fallible_function11_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function12_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function13_group39();

tl::expected<int, my_error_t> fallible_function12_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function13_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function14_group39();

tl::expected<int, my_error_t> fallible_function13_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function14_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function15_group39();

tl::expected<int, my_error_t> fallible_function14_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function15_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function16_group39();

tl::expected<int, my_error_t> fallible_function15_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function16_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function17_group39();

tl::expected<int, my_error_t> fallible_function16_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function17_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function18_group39();

tl::expected<int, my_error_t> fallible_function17_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function18_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function19_group39();

tl::expected<int, my_error_t> fallible_function18_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function19_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function20_group39();

tl::expected<int, my_error_t> fallible_function19_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function20_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function21_group39();

tl::expected<int, my_error_t> fallible_function20_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function21_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function22_group39();

tl::expected<int, my_error_t> fallible_function21_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function22_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function23_group39();

tl::expected<int, my_error_t> fallible_function22_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function23_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function24_group39();

tl::expected<int, my_error_t> fallible_function23_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function24_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function25_group39();

tl::expected<int, my_error_t> fallible_function24_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function25_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function26_group39();

tl::expected<int, my_error_t> fallible_function25_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function26_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function27_group39();

tl::expected<int, my_error_t> fallible_function26_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function27_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function28_group39();

tl::expected<int, my_error_t> fallible_function27_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function28_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function29_group39();

tl::expected<int, my_error_t> fallible_function28_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function29_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function30_group39();

tl::expected<int, my_error_t> fallible_function29_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function30_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function31_group39();

tl::expected<int, my_error_t> fallible_function30_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function31_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function32_group39();

tl::expected<int, my_error_t> fallible_function31_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function32_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function33_group39();

tl::expected<int, my_error_t> fallible_function32_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function33_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function34_group39();

tl::expected<int, my_error_t> fallible_function33_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function34_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function35_group39();

tl::expected<int, my_error_t> fallible_function34_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function35_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function36_group39();

tl::expected<int, my_error_t> fallible_function35_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function36_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function37_group39();

tl::expected<int, my_error_t> fallible_function36_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function37_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function38_group39();

tl::expected<int, my_error_t> fallible_function37_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function38_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function39_group39();

tl::expected<int, my_error_t> fallible_function38_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function39_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function40_group39();

tl::expected<int, my_error_t> fallible_function39_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function40_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function41_group39();

tl::expected<int, my_error_t> fallible_function40_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function41_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function42_group39();

tl::expected<int, my_error_t> fallible_function41_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function42_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function43_group39();

tl::expected<int, my_error_t> fallible_function42_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function43_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function44_group39();

tl::expected<int, my_error_t> fallible_function43_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function44_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function45_group39();

tl::expected<int, my_error_t> fallible_function44_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function45_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function46_group39();

tl::expected<int, my_error_t> fallible_function45_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function46_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function47_group39();

tl::expected<int, my_error_t> fallible_function46_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function47_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function48_group39();

tl::expected<int, my_error_t> fallible_function47_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function48_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function49_group39();

tl::expected<int, my_error_t> fallible_function48_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function49_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function50_group39();

tl::expected<int, my_error_t> fallible_function49_group39()
{
  auto result_a = non_trivial_destructor::initialize(39);

  if (result_a) {
    auto result_internal = result_a.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_a.error());
  }

  auto result_b = non_trivial_destructor::initialize(39);

  if (result_b) {
    auto result_internal = result_b.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result_b.error());
  }

  if (auto result = fallible_function50_group39(); result) {
    side_effect39 = side_effect39 + result.value();
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39;
}

tl::expected<int, my_error_t> fallible_function50_group39()
{
  if (auto result = non_trivial_destructor::initialize(50); result) {
    auto result_internal = result.value().trigger();
    if (!result_internal) {
      return tl::unexpected(result_internal.error());
    }
  } else {
    return tl::unexpected(result.error());
  }

  return side_effect39 + 1;
}
